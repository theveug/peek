<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
    <!-- Placeholder favicon: same accent-squircle + screen-share mark used for
         the topbar/settings-nav brand icon (.topbar-brand-icon), redrawn as
         plain shapes instead of the Material Symbols font glyph, since
         favicons are rasterized outside the page's own CSS/font context.
         #8c85f0 matches the existing violet-accent theme-color meta value.
         Swap this out once real branding lands — see project memory. -->
    <rect width="100" height="100" rx="30" fill="#8c85f0" />
    <rect x="22" y="28" width="56" height="36" rx="5" fill="none" stroke="#ffffff" stroke-width="6" />
    <rect x="40" y="70" width="20" height="6" rx="3" fill="#ffffff" />
    <path d="M50 34 L64 48 L55 48 L55 60 L45 60 L45 48 L36 48 Z" fill="#ffffff" />
</svg>
