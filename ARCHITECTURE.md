# Peek – Architecture & Design History

Human-facing design rationale and the full history of the visual redesign. This
file is **not** auto-loaded into an agent's context every session (unlike
`CLAUDE.md`) — it's the "how we got here" record, read on demand when a task
actually needs the backstory. The still-active rules and gotchas that must be
respected on *every* change live in `CLAUDE.md`'s "Key conventions"; this file is
the narrative behind them.

See also:
- `CLAUDE.md` — agent operating rules, file map, context-budget rules, active invariants/gotchas.
- `TODO.md` — active backlog only.
- `CHANGELOG.md` — completed work, most-recent first.

---

## Design principles

- **No info held on a server.** No accounts, no message/chat history persistence,
  no persisted room list. Sessions are in-memory only and disappear the moment the
  last peer leaves (`SessionManager.js`). Media is P2P wherever possible; even when
  a TURN relay is needed as fallback, it's blind-relaying encrypted bytes, not
  storing or inspecting anything.
- When weighing a new feature, check it against this first. Features that require
  persistent server-side storage (accounts, saved channel lists, chat history) are
  a deliberate exception, not a default — flag the tradeoff explicitly rather than
  building it in quietly.
- **No fake controls.** Don't ship a toggle/button that does nothing to match a
  mockup. Where the mockup drew interactive switches for things that are already
  always-true and non-configurable (P2P mesh, in-memory only, blind signalling),
  the app shows read-only info cards instead — a fake toggle is actively
  misleading. Same reasoning retired the mockup's "Zoom" button (no action to wire
  it to) and kept it wheel-only.

---

## Scaling ceiling & regulatory considerations (SFU migration, 2026-07-09)

*Not legal advice — this is engineering-team reasoning about the tradeoff, captured so
the SFU decision in `TODO.md` isn't made without this context. Get actual
jurisdiction-specific legal review before committing to an SFU design, especially if
transcoding/mixing/recording is ever added on top of it.*

The mesh architecture's scaling ceiling (see `TODO.md`'s "Scaling ceiling" section) is
real — full mesh is N×(N-1) connections, and quality tiers (`_qualityTier()`) only
shrink each connection's bitrate, they don't reduce the connection *count*. The actual
fix at large room sizes is an SFU (Selective Forwarding Unit): one upload per client,
fanned out server-side. That's a deliberate architectural shift away from "no info held
on a server," so before picking it up, it's worth being precise about what legal
exposure it would and wouldn't add — a concern raised directly by the user.

- **It's very unlikely to trigger age-verification/ID-check laws** (e.g. Australia's
  social-media minimum-age rules, in force since ~Dec 2025). Those regimes key off
  *product features* — persistent accounts, a social graph, content
  discovery/search across users, ongoing feed posting — not transport architecture.
  Most explicitly carve out services whose sole/primary purpose is messaging or
  voice/video calling. Zoom and Teams run centralized SFUs and aren't captured by
  these laws; the carve-out is about *what the product is*, not *how media moves*.
  Peek's no-accounts, ephemeral-room design is what keeps it on the "communication
  tool" side of that line, and an SFU migration wouldn't change that classification
  by itself — only adding social features (profiles, directories, discovery, feeds)
  would.
- **The real legal risk is different: carriage-vs-hosting classification.** Today's
  TURN relay (fallback only) is a blind byte-forwarder — it relays DTLS-SRTP
  encrypted packets without ever holding decryption keys, which is legally similar to
  a telecom carrier ("mere conduit" under the EU's e-Commerce Directive/DSA,
  common-carrier treatment under Australia's Telecommunications Act): no technical
  capability to inspect content, so no reasonable expectation to moderate it. A pure
  forwarding SFU (just picking which simulcast layer to relay per receiver) can
  preserve this property, especially combined with client-side E2EE via WebRTC
  insertable streams, so the server never holds keys. But an SFU that needs to
  **transcode, mix, or compose** streams (audio mixing for large calls, recording,
  server-side composition) typically has to decrypt server-side, even with nothing
  ever written to disk — and that's the point regulators/plaintiffs can run a
  "capability implies duty" argument: once you *can* inspect content, even
  transiently, you can be expected to. Australia's Online Safety Act industry codes
  around proactive CSAM detection are the concrete version of this, and they're
  framed around *reasonable technical capability*, not storage.
- **Keep privacy-law exposure and carriage-classification exposure separate** — they
  don't move together. Privacy-law exposure (GDPR-style, Australian Privacy Act) is
  about *storage* and stays minimal under a stateless SFU exactly as it does under
  the current mesh, as long as nothing persists server-side. Carriage/hosting
  classification is about *capability to inspect decrypted content*, and only shifts
  if a future SFU design needs to decrypt for transcoding/mixing/recording.
- **If/when the SFU migration is scoped**: favor a pure forwarding design (no
  transcode/mix/record) with client-side E2EE via insertable streams so the server
  never holds keys, and keep logging ephemeral/connection-metadata-only — that
  preserves the current legal posture. Recording or server-side composition
  features, if ever wanted, are the things that would actually need a fresh legal
  review, not the SFU migration on its own.

---

## Visual redesign (Phases 0-5 — all complete)

Based on a Claude Design handoff pack (`peek-design.zip`, originally from Downloads — `README.md` + `Peek.dc.html` prototype; the prototype is inline-styled preview-runtime HTML, **not** meant to be copied verbatim, just a spec for appearance/behavior). Phased: 0 decisions → **1 tokens/fonts (done)** → **2 screen re-skins — all sub-phases done (2a lobby, 2b top bar/Invite popover, 2c members sidebar, 2d stage/controls dock, 2e chat/files/polls panel)** → **3 settings panel (done)** → **4 removed the legacy glass/gradient background (done)** → **5 Background tint picker (done)**. DMs are explicitly out of scope (was a stray future-idea note in the pack, not a real ask). Phases 4-5 were new scope beyond the original pack — nothing further currently planned.

- **Phase 1 tokens**: `tailwind.css`'s `:root`/`body.light` now have a second, *additive* token set (`--bg1`..`--bg5`, `--border`, `--input`, `--t1`..`--t3`, `--accent`/`--accentH`/`--accentSoft`/`--accentSoft2`/`--accentText`, `--green`/`--amber`/`--red`) alongside the original `--color-*`/`--surface-*` tokens — original markup keeps working unchanged; only new surfaces (currently just the settings overlay) are built on the new set. Accent tokens are runtime-only, owned entirely by `AccentManager.js` (static token file values are just the pre-JS default/no-FOUC fallback, Violet hue 286).
- **Fonts**: self-hosted Public Sans + Fira Code (single variable-weight `.woff2` each, covers all needed weights) + Material Symbols Rounded (`public/assets/fonts.css`, files in `public/assets/fonts/`). `html, body` now default to Public Sans; `.font-mono-app` utility class and `.chat-markdown code` use Fira Code — **most of the app still hasn't been swept to actually use `.font-mono-app`** for room codes/latency/etc. Material Symbols is the *unsubsetted* full icon set (~5.3 MB, one-time cached download) — subsetting to just the ~30 icons actually used is a worthwhile follow-up, not done yet.
- **Phase 3 settings panel**: `#settings-modal` went from a small centered modal (the original bug report: too many fields, no scroll region, clipped on short viewports) to a full-frame overlay — 262px left nav (5 sections) + a content column with its own `overflow-y-auto`. Owned by the new `public/client/SettingsPanel.js`, same `#settings-modal` id/open-close contract so nothing else had to change. **Every field applies + persists immediately on change now — there's no Save/Cancel step anymore** (matches the design spec's interaction model; deliberate behavior change from the old form-submit pattern). Nickname change fires on `change` (blur), not `input`, so it doesn't broadcast a nickname update per keystroke.
  - **One settings surface everywhere**: the standalone `/settings` page (`settings.html`/`settings.js`) was retired — it was already dead (nothing linked to it) and out of sync (missing several fields). `lobby.html` now carries its own copy of the settings-overlay markup + a gear button, and instantiates `SettingsPanel` with no `ui`/`peerManager` (both optional in the constructor) so pre-room settings still load/persist to `localStorage` without erroring on the parts that need a live connection (status picker, live quality re-apply, nickname broadcast — all guarded with `?.`).
  - **Privacy & P2P section deliberately deviates from the mockup**: it shows read-only info cards (not interactive toggles) for things that are already always-true and non-configurable (P2P mesh, in-memory only, signalling server blind to content) — the mockup draws these as toggle switches, but shipping fake toggles that do nothing would be actively misleading. The one *real* toggle there is `autoAcceptFiles`.
  - **Gotcha already hit**: the custom `.settings-switch` toggle (checkbox + a `.settings-switch-track` sibling span for the visual pill/thumb) needs `pointer-events: none` on the track — without it, the visually-on-top track span intercepts every click meant for the checkbox underneath, so the toggle looks clickable but silently does nothing. Caught by `tests/file-consent.mjs` failing to check the box, not by eyeballing it.
- **Quick room settings popover (2026-07-04, follow-up to Phase 3)**: not every setting deserves a trip through the 5-section full panel — status, `autoAcceptFiles`, `followActiveSpeaker`, and screen/cam quality are things people plausibly want to flip mid-room, so they also live in a lightweight popover off `#quick-settings-button` (`public/client/QuickRoomSettings.js`). It's a second UI surface only — same `localStorage` keys and `PeerManager` methods as `SettingsPanel.js`, no duplicated data/validation logic, and its "All settings" link hands off to the real `SettingsPanel.open()`. The status control changed shape here too: the old inline 3-button segmented picker (still what `SettingsPanel.js`'s Profile section shows) vs. the popover's `#quick-status-trigger`/`#quick-status-menu` click-to-open dropdown — same `peerManager.setManualStatus()` underneath either way. Screen/cam quality in the popover only exposes resolution (not fps) to keep it compact.
- **Phase 2a lobby re-skin (2026-07-04)**: `public/lobby.html` rebuilt from a small `max-w-md` card onto Phase 1 tokens — a 900px-wide column: hero (squircle accent icon + "Start a room" + subcopy, blurred `--accentSoft` radial glow behind it via `.lobby-glow`), a 2-col `.lobby-card` grid (Create/Join), the Saved Rooms list re-skinned into a `.lobby-recent-grid` of cards, and a static 4-item privacy footer. New CSS lives in tailwind.css's `/* ===== Lobby (redesign, Phase 2a) ===== */` section.
  - **Stepper replaces the max-participants `<select>`**: `#create-max-peers` is now a readonly `<input type="number">` (min 2, max 12, default 6) driven by `#create-max-peers-dec`/`-inc` buttons — same id/value contract as before, so `#create-form`'s submit handler didn't need to change.
  - **Join input gained paste-link support**: a `paste` handler takes the pasted text, strips non-alphanumeric, and keeps the trailing 5 characters — so pasting a full invite link (`https://peek.app/r/AB12C`) extracts the room code instead of concatenating the whole URL.
  - **Recent room cards**: `savedRooms.js`'s `saveRoom()` stamps a `savedAt: Date.now()` on every upsert; `lobby.html` calls `saveRoom()` again on a successful "Rejoin" click so lastActive stays current. Each card's avatar hue is computed client-side from the room's code+label via a string hash (`hueFromString()`) — deterministic per room, nothing persisted.
- **Phase 2b top bar + Invite popover (2026-07-04)**: the room page had **no persistent top chrome at all** before this — just a floating `#room-header` pill plus the hover-reveal bottom `#controls` bar. User chose the mockup's literal always-visible 58px bar over an enhanced floating-pill alternative. New `.topbar` row inserted as the first child of the root `flex flex-col h-screen` wrapper (`public/index.html`), so the existing `flex-grow` row below it just shrinks via flexbox.
  - **Left (as originally shipped)**: brand + room-state pill (`#topbar-room-name`/`#topbar-room-lock`/`#topbar-room-code`, populated via `UIController.setRoomMeta()` from `App.js`'s `'init'` handler). **Right**: peer-count+signal pill (`#topbar-peer-count`/`-cap`, `#topbar-signal-wrap`/`#topbar-signal-icon` — renamed from `#topbar-latency`/`-wrap` 2026-07-08 for the signal-bars redesign, see the "Top-bar mesh signal icon" entry below), the Invite popover, the **relocated** `#settings-button`/`#quick-settings-button` (moved out of the bottom controls dock — pure DOM relocation, same ids), and an identity pill (`#topbar-identity-*`). **Both pills were removed from `public/index.html` by a 2026-07-07 commit (`147091a`) and stayed gone** — `setRoomMeta()`/`setHasPassword()`/`updateMeshSignal()` are intentionally-inert no-ops (null-guarded) as of this writing. This was flagged mid-session 2026-09-06 as if it were an accidental reindent-commit casualty and briefly restored; the owner clarified the removal was deliberate (the room-name/code/peer-count/signal info duplicates what's shown elsewhere, and is meant to get a more meaningful home later, not this generic pill treatment) and it was re-removed. **Left, as of 2026-09-06**: brand + the new deployment trust badge (`#topbar-trust-icon`, see "Trust-tier indicator" below), placed directly after the brand icon rather than grouped with either pill.
  - **`#share-button`'s old silent instant-clipboard-copy behavior was removed entirely** — replaced by `InvitePopover.js`, which shows the room code and a direct link with their own Copy buttons and a toast for feedback.
  - **Mesh latency is a new data point**: `PeerManager._pollConnectionStats()` already fetched per-peer RTT every 3s but discarded the raw ms; now it accumulates valid `rtt * 1000` samples and calls `UIController.updateMeshLatency(avgMs)` once per poll tick — reuses the existing timer.
  - **Server plumbing**: `WebSocketServer.js`'s `'init'` payload now includes `hasPassword`/`maxPeers` (already returned by `getSessionMeta()`) so the client can render the lock icon and cap without a new endpoint.
  - **Cascade-layers gotcha (origin of the standing rule in CLAUDE.md)**: toggling a `.material-symbols-rounded` icon's visibility via the Tailwind `.hidden` utility silently didn't work — Tailwind v4 wraps utilities in CSS `@layer`s, and any unlayered rule beats any layered rule regardless of specificity/order; `fonts.css`'s `.material-symbols-rounded` is unlayered, so it always won over `.hidden`'s layered `display: none`. Fixed by toggling `element.style.display` directly.
- **Phase 2c members sidebar (2026-07-04)**: `#members-sidebar`/`_createParticipantCard()` re-skinned onto Phase 1 tokens — square (11px radius) avatars with a deterministic per-peer hue (`UIController._hueFromString()`, same algorithm as `lobby.html`'s but duplicated locally since the two files are separate unbundled entry points), a compact right-side column (signal bars over mic icon, reusing existing `_signalBarsSvg()`/`_micOnSvg()`/`_micOffSvg()`), and a static "Fully meshed" trust card.
  - **Self card moved out of the visible list into a new footer strip** (avatar/nickname/status dropdown/settings gear) — explicit user choice. **The self card is still created in the DOM** (`addSelf()` unchanged) so the existing peerId-keyed update methods keep working unmodified — only visually hidden via `.members-panel .participant-card[data-self="1"] { display: none; }`. Removing it from the DOM entirely would have broken those methods' `!el → addParticipant(peerId)` fallback paths.
  - **Member rows have no hover/click affordance** — the mockup's rows open a private DM, but DMs are out of scope, so `.members-panel .participant-card:hover { background: transparent; }` overrides the shared hover rule.
  - **New `public/client/SidebarIdentity.js`** wires the footer's status dropdown + settings gear — mirrors `QuickRoomSettings.js`'s pattern under new ids so it doesn't collide with the top bar's identical-shaped dropdown. Deliberate second functional entry point, not a redundant copy — the top bar's identity display is glance-only, the sidebar footer adds real click-to-change-status.
  - **Mobile gotcha (origin of the standing rule in CLAUDE.md)**: the footer strip rendered underneath the fixed bottom `#controls` bar (`z-index: 90` vs the drawer's `80`). Fixed with `padding-bottom: calc(11px + 64px + env(safe-area-inset-bottom))` on `.sidebar-identity` in the `@media (max-width: 767px)` block, not a z-index change.
- **Phase 2d stage + controls dock (2026-07-04)**: re-skinned the grid/focus stage and the bottom controls dock. Scoped down via two explicit user decisions: no focus-view filmstrip (focus view is single-stream only today) and no "React" floating-emoji button (doesn't exist).
  - **New stage header row** (`#stage-header`, first child of `#videos`): a Grid/Focus segmented control (`#stage-view-grid`/`#stage-view-focus`, calling `ui.setViewMode()` directly), an active-stream count (`watchedTiles.size / maxWatchedTiles`), and a quality readout that only appears while screen-sharing (`PeerManager.getScreenQualityLabel()`, pushed via `ui.updateStageQuality()` from `startSharing()`/`stopSharing()`).
  - **Grid tiles** (`_buildGridCell()`): rounded corners, a mic icon in the name label (new `this._micEnabled` map keyed by real peerId, updated in `updateParticipantMic()`'s existing choke point), a screen-share resolution badge from the `<video>`'s own `videoWidth`/`videoHeight` on `loadedmetadata`, and the speaking ring (`setSpeaking()` now toggles `.status-talking` on any grid tile matching `peerId` or `peerId-cam`, not just the participant-list avatar).
  - **Focus view**: a "`{name}` is sharing" pill and quality caption, populated in `focusStream()`/`_updateFocusMeta()` from existing data. Zoom/pan stays wheel-only.
  - **Controls dock**: circular pill buttons (`.dock-btn`, 48px); the Leave button becomes a wide labeled red pill. **`#grid-toggle`/`#grid-button` removed** (superseded by the stage-header toggle). **`#theme-toggle` relocated into the top bar** (`.topbar-icon-btn`, same id/handler). A mic-mode badge (`#mic-mode-badge`, "PTT"/"PTM") surfaces `localStorage.micMode` via `ui.updateMicModeBadge()`.
  - **Cascade-layers gotcha hit again**: `updateShareButton()` toggled `#stop-share-button` via `.hidden`, which broke once its markup switched to the custom unlayered `.dock-btn` class (which sets `display: flex`). Fixed with inline `style.display` — this is the second instance that established the standing rule now in CLAUDE.md.
- **Phase 2e chat/files/polls panel (2026-07-04) — Phase 2 complete.** Nothing here was a new feature — reactions, replies, code-block copy, file cards, and poll voting bars already existed; this was a `bg-indigo-*`/`border-indigo-500` → design-token sweep plus a few component restyles. **Polls stay an in-chat card + modal**, not promoted to a third tab like the mockup (an IA change, not a re-skin).
  - **Tab bar** (`#tab-chat`/`#tab-files`): old underline tabs replaced with a pill-segmented control (`.panel-tabs`/`.panel-tab-btn.active`). `UIController._switchTab()` simplified from 8 conditional class toggles to one `.active` toggle each.
  - **Chat composer** (`#chat-input`): rebuilt into a rounded pill (`.chat-composer`) with `--bg5` circular icon buttons. **Added a dedicated send button** (`#send-message-btn`, wired to the same `sendMessage()` Enter already called) — the app previously had no visible send affordance at all, a real mobile-usability gap.
  - **Files tab**: added a persistent "Click to send a file" dropzone card (`#files-dropzone-card`) wired to the existing hidden `#file-input`. Note a pre-existing gap flagged here: drag-and-drop and paste are wired at the Chat tab's DOM level and don't fire while the Files tab is showing. Picking a file from this card calls `ui._switchTab('chat')` so the user sees what they staged.
  - **Chat avatars**: `.chat-avatar` `border-radius` changed from circle to `6px`. The per-name color algorithm (`ChatUI._colorForName()`) was left unchanged.
  - **Poll modal + option bars**: `focus:ring-indigo-500/50` replaced with a `.poll-input:focus` rule; option bars swapped hardcoded indigo utilities for `--accentSoft`/`--accent` via `.poll-option-voted`/`.poll-option-fill`/`.poll-option-hoverable`.
- **Phase 4 — removed the legacy glass/gradient background (2026-07-05).** With Phase 2 done, the chat panel, members sidebar, controls dock, panel-toggle tabs, and update banner were still using the pre-redesign glass-morphism look — `backdrop-filter: blur()` over the animated gradient canvas (`GradientBackground.js` + `#gradient-canvas`). Confirmed via AskUserQuestion: remove entirely to match the mockup's flat solid panels.
  - **Deleted `public/client/GradientBackground.js`** and its two call sites (`App.js`, `lobby.html`) plus the `<canvas id="gradient-canvas">` from both pages.
  - **Flattened every glass surface** onto solid tokens: `.chat-panel`/`.members-panel` → `background: var(--bg2)`; `.dock-bar`/`.panel-tab` → `background: var(--bg3); border: 1px solid var(--border)`; update banner likewise. Removed the dead `.surface-glass`/`.controls-glass` classes, `--surface-glass`/`--controls-glass`/`--controls-border`/`--gradient-1..4` tokens, the `#gradient-canvas` rule, and two orphaned selectors.
  - **Real bug found during verification**: the canvas had quietly been the page's *only* source of a background color (`body`/`html`/`.lobby-page` never declared one), so removing it left plain white showing through the lobby and stage. Fixed with `background: var(--bg1)` on the base `html, body` rule. Any future removal of a full-page background element should check whether anything else declares a base page color.
- **Phase 5 — Background tint picker (2026-07-05), new scope from an earlier "Mesh" design prototype (pre-dates the repo, shown via screenshot).** Adds a 6-swatch "Background Tint" picker to Settings → Appearance (Violet/Slate/Plum/Forest/Sand/Ocean), between Accent Color and Preview — shifts the neutral `--bg1..5`/`--border`/`--input` scale's hue independently of the accent. Built as a near-exact structural copy of `AccentManager.js` (`public/client/BackgroundManager.js`) so a user follow-up — "how easy to add more presets myself?" — has a good answer: one line in a `{ name: hue }` object, no CSS/rebuild.
  - **Real bug found during verification (origin of the standing rule in CLAUDE.md)**: the first implementation applied the override on `documentElement.style` (matching `AccentManager.js`) and silently didn't work while light theme was active — `--bg1..5`/`--border`/`--input` are the first redesign tokens redeclared per-theme (`body.light`), unlike accent tokens which only live in `:root`, and a directly-matched declaration on `<body>` beats an inherited value from `<html>`. Fixed by overriding on `document.body.style` instead.

---

## Feature narratives moved out of CLAUDE.md's Key Conventions (2026-07-15 housekeeping pass)

These were originally full entries in CLAUDE.md's "Key conventions." Each has been reduced there to a one-line pointer (the still-load-bearing rule, if any) referencing this section — moved here verbatim rather than trimmed, since the point of the split is that this file carries the "how we got here" story and CLAUDE.md only carries the standing rule.

- **Draggable self-view PiPs (2026-07-07)**: `#self-view`/`#self-cam-view` (your own screen/cam preview, `UIController.addStream()`) are drag-to-reposition (Discord-style) via `UIController._makeDraggable(el, storageKey)`, using Pointer Events (`pointerdown`/`pointermove`/`pointerup` + `setPointerCapture`) so mouse and touch share one code path. Position is a personal layout preference, not session state, so it's persisted to `localStorage['pipPosition:self-view']`/`['pipPosition:self-cam-view']` and restored on the next stream/reload — unlike `peerVolumes`, there's no peerId-reassignment problem here since the storage key is the PiP's fixed role (`self-view`/`self-cam-view`), not a peerId. A dragged element gets `dataset.dragged = 'true'`, which `_updateSelfViewPositions()` (the existing right-edge auto-stacking for when both screen+cam PiPs are visible at once) checks and skips — otherwise every stream add/remove would snap a manually-placed PiP back to the corner. `window.resize` reclamps any dragged PiP back on-screen (`_reclampDraggedPip`) in case a saved position from a larger window is now off-screen.
- **Top-bar mesh signal icon (2026-07-08)**: the peer-count pill's old "Xms · mesh" text is now the same colored signal-bars icon used per-participant in the members list (`UIController._signalBarsSvg`/`.participant-signal-icon`) — `UIController.updateMeshSignal(ms, tier)` (renamed from `updateMeshLatency`, `#topbar-signal-wrap`/`#topbar-signal-icon`), with the exact ms + quality label only as a hover tooltip. `tier` is the *worst* per-peer connection quality across the mesh (`PeerManager._pollConnectionStats`'s `_qualityRank` ranking), not an average — one bad connection among several good ones should still show as bad, not get diluted out.
- **Trust-tier indicator (2026-09-06)**: a small `#topbar-trust-icon` shield badge in `.topbar-left`, directly after the brand icon — first step of an open-ended (not yet decided) conversation about whether Peek should ever support hosted/accounts-based deployments alongside its pure-P2P/no-accounts default — independent of that decision, the owner wanted something that makes what's already true about a given server visible to an end user, not just documented in `CLAUDE.md`. `server.js` builds a deployment-level `trust` object once at startup: `accounts`/`serverSideHistory` are hardcoded `false` (neither feature exists anywhere in the app yet — deliberately not wired to a speculative env flag for a feature that doesn't exist), `debugLogging`/`mediaRelayConfigured` are real, sourced from the already-existing `DEBUG_ENABLED` flag (`utils/Debug.js`) and `!!turnConfig`. Sent once per socket as a sibling field on the WS `init` message (alongside `iceServers`/`buildId`, both also deployment-level) — deliberately *not* folded into `SessionManager.getSessionMeta()`, which is per-room state. `App.js`'s raw `'init'` handler reads it (not `PeerManager.handleSignal()`, since it's not mesh/moderation state) and calls `UIController.setTrustTier(trust)`, which composes a `dataset.tip` sentence and reveals the badge — it starts `hidden` so it's never on screen making a claim before a real `init` has confirmed it. Tooltip-only in v1, same passive pattern as the mesh signal icon above; a richer click-to-expand popover was considered and deliberately deferred, not forgotten. **Deliberately not grouped with the old room-state/peer-count pills** — see the correction in the Phase 2b entry above: those pills' 2026-07-07 removal turned out to be intentional (not the accidental drop it first looked like), so the badge stands alone rather than rejoining them. Server-side authority guarded by `tests/trust-tier.mjs`.
- **Grid/focus tile overlay cleanup (2026-07-08)**: the grid tile's "Stop watching" control used to be a text pill at `top-right`, directly on top of the screen-share resolution badge (`.grid-tile-badge`, also `top-right`) — the two visibly overlapped. Fixed by giving each corner exactly one thing: `.grid-tile-stop-btn` (icon-only, red `close` glyph, matches `--red`/hover-fills-red like `.participant-mod-kick`) owns top-right, `.grid-tile-badge` moved to bottom-right, `.grid-tile-name` (mic icon + nickname) stays bottom-left but now has the same dark pill background as `.focus-quality-caption` instead of a bare text-shadow, so both views read as one consistent label style. Focus view previously showed the peer's name **twice** — an accent-colored `.focus-sharing-pill` banner top-left ("● Name is sharing") *and* a separate `.focus-quality-caption` pill bottom-left ("Name · WxH") — collapsed into one bottom-left pill (`#focus-quality-caption` now holds both the live dot and `#focus-caption-text`); `_updateFocusMeta()` targets `#focus-caption-text` instead of setting `caption.textContent` directly (that would have wiped out the dot span). Focus view also gained the same stop-watching affordance grid tiles already had — `#focus-stop-button`, grouped with the existing PiP button in `#focus-icon-btn-group` (`.focus-icon-btn`s are no longer individually `position:absolute`; the group wrapper owns that now, so multiple icon buttons stack horizontally instead of colliding). Clicking it calls `_unwatchTile(focusedPeerId)` **without** clearing `focusedPeerId` or falling through to `updateLayout()`'s auto-refocus branch — in a 2-person room `remoteStreams[0]` would still resolve to the same peer, so an unconditional refocus would instantly re-watch the very stream you just paused. Instead `_renderFocusWatchState()` toggles a dedicated `#focus-paused-overlay` (reuses the exact grid-tile paused-overlay classes/copy — "Click to watch · stream paused · saving bandwidth" — for one consistent paused look everywhere) over the video, hiding the caption/icon-buttons until the overlay itself is clicked to resume (`_watchTile` + restore `srcObject`). Called from `focusStream()`, the stop/resume handlers, and every `updateLayout()` focus-branch pass, so it can't drift out of sync with `watchedTiles`.
- **"Me" sentinel removed (2026-07-13)**: the local echo of chat/poll/file messages used to pass the literal string `'Me'` as `sender` — displayed as-is, so your own messages showed "Me" instead of your actual nickname regardless of what you'd set. Every self-echo call site now passes the resolved nickname (`localStorage.getItem('nickname') || 'Anonymous'`) instead, while self-vs-remote detection moved fully to the already-existing explicit `isSelf` boolean param (added to `addPollMessage`/`addFileMessage`, which previously inferred it by comparing `sender === 'Me'` — a latent bug, since a remote peer naming themselves "Me" would have false-positived as self). `_replyQuoteHtml()`'s self-color check compares against the current nickname the same way. This also fixed `getMessageHistory()`'s recap transcript, which was showing "Me" for the same reason.
- **Larger, Discord-style mic/deafen status icons (2026-07-13)**: `_micOnSvg()`/`_micOffSvg()`/`_deafenSvg()` (all shared choke points — used by both participant cards and grid tiles) bumped from `w-2.5`/`w-3` (10-12px) to `w-4` (16px); `_signalBarsSvg()` scaled up to match (12x10 → 16x13) so the passive-icons row stays proportional rather than mixing icon sizes. `_micOffSvg()` was previously just a generic "X" shape with no mic imagery at all (easy to mistake for a close/dismiss icon) — now it's the same mic glyph as `_micOnSvg()` plus a diagonal slash line, so "muted" reads as "the mic icon, slashed" the way Discord/Zoom/Teams do.
- **Full-height clickable chat composer (2026-07-13)**: clicking anywhere inside `#chat-input` (the composer bar's padding, the gap between the textarea and its buttons — not just the textarea's own rendered box, which can be much shorter than the bar) focuses `#message`. Wired in `App.js` as a single click listener on the container, excluding clicks on `.composer-plus-wrap`/`#send-message-btn` via `e.target.closest()` so those buttons' own behavior isn't hijacked into a focus-and-nothing-else.
- **Solid-red mute/deafen dock buttons + deafened avatar ring (2026-07-14)**: `App.js`'s `updateMicUI()`/`setDeafenUI()` toggle the pre-existing `.dock-btn-active-red` class (previously only ever applied statically to `#stop-share-button`) onto `#mic-toggle`/`#deafen-toggle` while muted/deafened — Discord-style "unmistakably off" feedback, reusing the existing red-fill style rather than inventing a new one. Separately, `UIController.updateParticipantDeafen()` now also toggles a new `.status-deafened` class on the peer's avatar (participant card, plus `#topbar-identity-avatar` for the local user — same dual-write `setSpeaking()`'s ring already does) — a **solid, non-pulsing** red ring, using the same box-shadow-vs-`.avatar-img`-drop-shadow dual-selector split as `.status-talking`'s speaking ring (tailwind.css) so a transparent/non-square custom avatar's ring still hugs its real silhouette rather than a mismatched square. Applies to every peer (broadcast via the existing `deafen-status` message), not just the local user.
- **lobby.html's Mic mode picker was missing "Voice Activity" (2026-07-14 bug fix)**: only had Toggle/Push to Talk/Push to Mute plus the push-to-talk/mute keybind row — the 4th mode and its sensitivity/hold-time sliders (pure `localStorage` config, just as settable pre-room as the other three) were never copied over when `lobby.html`'s settings markup was split from `index.html`'s. Fixed by adding the missing button + both sliders, matching `index.html` exactly. The live mic-level meter row is still deliberately room-only (it needs an actual mic stream `SettingsPanel._startMicMeter()` has no `peerManager` to read on the lobby).
- **Panel toggle tabs flush against their panel edge (2026-07-08 fix)**: `.chat-tab`/`.members-tab` are `position:absolute` children of `#videos` (the stage), positioned via `left:0`/`right:0` — but `#videos` itself starts 4px in from the actual chat/members panel edge, because the 4px-wide `.chat-resize-handle`/`.members-resize-handle` sits between them as a flex sibling, so the tabs used to float with a visible 4px gap instead of reading as attached. Fixed by pulling them back over that gap (`left:-4px`/`right:-4px`) and giving `#videos` a `z-index:61` (it had none of its own) so the tabs — now intentionally overlapping the handles by 4px — win pointer events there instead of losing to the handles' own `z-index:60`. The handles disappear (`display:none`) once their panel is hidden/collapsed, so `body:has(#chat.hidden) .chat-tab`/`body:has(#members-sidebar.collapsed) .members-tab` zero the offset back out in that state, and the mobile media query (no resize handles at all there) resets both to `0` too.

### Additional entries moved 2026-07-19 (second housekeeping pass)

Same treatment as above — moved verbatim from CLAUDE.md's "Room moderator" Key Conventions entry, which had grown into the largest single block in that file and substantially overlapped `CHANGELOG.md`'s own Rooms/Moderation entries for the same work. The standing rules that remain load-bearing (token-not-role identity, the two-tier isCreator/isModerator permission split, server-side-only enforcement, kick-vs-ban mechanics) stayed in CLAUDE.md, trimmed; these two sub-bullets were pure incident history with no forward-facing rule left to extract.

- **Lazily-created rooms get an owner too (2026-07-11 fix)**: joining a code with no live session (a dead saved room, a shared link after everyone left, a typo'd code) lazily creates the room — but used to create it *ownerless*, since only `/api/create-room` minted tokens, so no kick/ban/promote was possible for that room's entire life. `WebSocketServer.js`'s `join` case now mints a `creatorToken` whenever the join is lazily creating the session and the joiner didn't present one, and returns it in the `init` payload; `App.js` persists it to `sessionStorage` (its module-level `creatorToken` is a `let` for this — reconnects in the same tab must re-claim with it). Whoever revives a dead code owns the revived room — same trust level as the already-accepted saved-code-squatting behavior. **Same fix, same commit**: `SessionManager.addPeer`'s lazy-create used to build its own session object literal that had drifted from `createSession`'s shape (missing `bannedIps` — `recordBan` threw and killed the server for bans in lazily-created rooms); it now routes through `createSession`, deliberately the only place a session object is built. Guarded by `tests/lazy-room-owner.mjs` (`npm run test:lazy-owner`).
- **Ownership also has to survive a closed browser/restarted PC even when the room never emptied (2026-07-11, second owner-reported gap)**: the fix above only helps when the session actually died and got lazily recreated. If another peer kept the room alive the whole time, the session never dies, so nothing re-mints a token — the *original* owner's only proof of ownership was `sessionStorage['creatorToken']`, which is gone the moment the tab/browser/PC restarts, permanently losing the crown even though the room they created is still right there with people in it. Fixed with `public/client/ownerTokens.js`, a `localStorage`-backed map of room code → creator token (survives a closed browser, unlike sessionStorage) — capped at 100 entries, LRU-evicted by `savedAt`. `App.js`'s `creatorToken` now falls back to `getOwnerToken(sessionId)` when sessionStorage has nothing at load, and mirrors into `setOwnerToken(sessionId, token)` any time it holds a token from *any* source (sessionStorage, a freshly-minted lazy-owner token, or the localStorage fallback itself) — so both stores stay in sync regardless of which one supplied it. Deliberately **not** cleared on Leave Room (unlike the sessionStorage entries, which are cleared there specifically to stop a flat, non-room-scoped key leaking into whatever room gets joined next in the same tab — see the socket-lifecycle note above) — leaving a room isn't relinquishing ownership of it, and since this store is correctly keyed per room code, it carries no cross-room leak risk to begin with. Same accepted trust level as `savedRooms.js` already storing plaintext room passwords in localStorage. Guarded by `tests/owner-token-persistence.mjs` (`npm run test:owner-persist`): creates a room normally via the lobby flow (not a lazy join), a second peer joins and stays connected (keeping the session alive), the owner's `sessionStorage` is cleared to simulate the restart, and after a reload the crown returns *and* an actual kick still works — proving the reclaim is server-enforced, not just a client-side icon.
- **Background blur's WASM/CSP + SIMD-vendoring bugs (2026-07-09)**: hit while building the opt-in webcam background blur (`VirtualBackground.js`, self-hosted MediaPipe Selfie Segmentation — see `public/assets/vendor/mediapipe/`'s project-structure entry for the standing rule on why both WASM variants are vendored). **Bug 1**: WASM compilation is blocked outright under this app's strict CSP (`script-src 'self'`, no eval) — fixed by adding `'wasm-unsafe-eval'` (a narrower CSP Level 3 keyword permitting only WASM compile/instantiate, not general `eval()`/`Function()`) to `server.js`'s CSP; `'unsafe-eval'` itself remains deliberately absent. **Bug 2**: vendoring only the SIMD WASM variant (smaller download) 404'd in this project's actual test environment — its headless Chromium didn't take the SIMD path at runtime, so both the SIMD and non-SIMD WASM+glue pairs ended up vendored (harmless: only one pair is ever fetched per session, runtime-detected, so this only costs one-time server disk space, not per-user bandwidth). Verified via a scripted check: confirmed the outgoing track becomes a genuine canvas-capture stream (opaque generated label, not the raw device's label) with zero console errors and zero failed vendor-asset requests, and that toggling blur off while the camera stays on live-swaps back cleanly.
- **Mic noise suppression's CSP requirement + verification (2026-07-10)**: same shape as background blur above but for audio (`NoiseSuppressor.js`/`rnnoise-worklet-processor.js`, self-hosted RNNoise WASM — see `public/assets/vendor/rnnoise/`'s project-structure entry). Requires `worker-src 'self'` in `server.js`'s CSP (added alongside the existing `'wasm-unsafe-eval'`) since `AudioWorkletGlobalScope` runs in its own realm. Verified via scripted checks (single-page: `addModule()`/`createMediaStreamDestination()` actually fire, zero console errors; two-peer: `replaceTrack()` fires on the real `mic-audio` sender and the connection survives toggling live) plus `tests/noise-suppression-lifecycle.mjs` for the two easy-to-regress invariants (mute state carried across the swap, raw stream never stopped while in use).
- **Dock-button quick popovers, full control breakdown (2026-07-14)**: the screen-share, webcam, and mic dock buttons each gained a `.dock-caret` (same caret-triggers-popover pattern as the pre-existing `#deafen-volume-caret`, generalized into `App.js`'s `wireQuickPopover()`/`wireSegmentedPicker()` helpers since there are now 4 of these instead of 1) opening a `.dock-quick-popover` (a new vertical-stack sibling of `.dock-volume-popover` for content that doesn't fit a single icon+slider+value row). Screen/cam popovers hold Resolution + Frame rate segmented pickers, reusing the exact `screenShareRes`/`screenShareFps`/`camRes`/`camFps` localStorage keys and `PeerManager.applyQualitySettings()`/`applyCamQualitySettings()` the full Settings panel and QuickRoomSettings' own quick pickers already use (resolution option set matches QuickRoomSettings' reduced quick-tier choices, not the fuller Settings-modal set; cam fps options are `10/15/24/30` to match the canonical cam fps picker, not screen's `10/15/30/60`). The mic popover holds a Mic mode segmented picker plus (mode-dependent) a keybind-capture input and sensitivity slider — same three controls as Settings' Audio & Mic mic-mode section. Its keybind capture shares `SettingsPanel`'s `_keybindListening` flag via a new `setKeybindListening()` setter (previously only exposed via the read-only `isKeybindListening()`), so the global push-to-talk/push-to-mute/deafen keydown listeners correctly pause while capturing from this second capture surface too. Every picker re-reads its localStorage key and resyncs `.active` state on each popover open (`onOpen` callback), since another surface may have changed it while this one was closed. The pre-existing `#deafen-volume-caret`/`#deafen-volume-popover` was refactored onto the shared `wireQuickPopover` helper specifically so it participates in the same mutual-exclusion set (see CLAUDE.md) rather than being a 5th one-off implementation.
- **Concurrent file transfers to the same peer, full bug narrative (2026-07-09 fix)**: `handleDataChannelMessage`'s binary branch silently assumed at most one file is ever in flight *from a given sender* at a time. That assumption held while multi-file sends were rare, but the caption+files grouped-message feature made "several files in one batch" a normal, everyday action — and `sendFileToAll()` never awaited its own transfer completion (it kicks off `_sendFileToPeer` and returns), so a batch's accepted chunk-streams to the *same* peer ran concurrently, interleaved on one data channel. The receiver's `.find()` always resolved to the same (first-started) transfer for every chunk regardless of which file it actually belonged to, corrupting both files' bytes and often tripping the declared-size guard (`t.receivedBytes > t.fileSize + 65536`) on whichever file's real data got shorted — this is exactly the bug behind a batch showing broken images plus a spurious "exceeded declared size" block on one file, even though every file uploads fine alone. Fixed by serializing actual chunk transmission per peer (`PeerManager._sendQueues`), not by touching the receive-side demux — see CLAUDE.md for the resulting standing rule. Verified this exact test (`tests/file-transfer-concurrency.mjs`, 4 distinguishable same-size-ballpark files in one batch, diffing actual downloaded bytes against the originals) fails against the pre-fix code both via a false size-block and via silent corruption, before confirming the fix passes it cleanly across repeated runs.
- **Own-avatar-never-showing bug fixed 2026-07-13** (owner-reported: "i see everyone else's but not mine"): `broadcastAvatar()` only ever *sends* your avatar to other peers — nothing looped it back to populate your own `peerAvatars` entry at load time. `SettingsPanel._wireAvatar()`'s handlers already apply a *live in-room change* locally (`this.ui.updateParticipantAvatar(this.peerManager.peerId, dataUrl)`, right alongside the broadcast call), but that only covers changing your avatar *while already connected* — it does nothing for an avatar set on a previous visit (or in the lobby, pre-connection, where `SettingsPanel` has no `peerManager` at all) that should still apply the moment you join. `UIController.addSelf()` now seeds `this.peerAvatars` straight from `localStorage.getItem('avatarDataUrl')` before its first render pass (mirroring how it already reads `localStorage.getItem('nickname')` directly rather than waiting on a broadcast round-trip) — same fix covers the topbar identity pill, your own participant card, and now your own chat messages, all three read through the same map. Verified with a raw-`ws` forged-peer test (mirrors `tests/chat-edit-delete.mjs`'s authz-spoof style) confirming a malicious `svg+xml` payload and an oversized payload both get silently dropped with zero script execution, while a well-formed same-shape payload from any sender (not just our own canvas output) is correctly accepted — the allowlist is format-based, not sender-based, by design.
- **Transparent-background avatars + shape-hugging speaking ring (2026-07-13, owner-reported: some avatars use non-square shapes on a transparent background, but the container's solid initials-color square showed through the "transparent" parts, and the speaking ring traced that square container instead of the image's actual silhouette)**: `_renderAvatarInto()` now clears the container's `background` to `'transparent'` when showing an image (restoring the colored square via `_avatarSquareColor()` only when falling back to initials), and `ChatUI._avatarHtml()`'s image branch drops its `background` inline style the same way — both were previously leaving the initials-mode color square active underneath the image regardless. The speaking ring (`.status-talking`, `tailwind.css`) now has two mutually-exclusive rules keyed on `:has(.avatar-img)`: everything without an avatar image (initials avatars, grid tiles — real video, always a genuine rectangle) keeps the original `box-shadow`-based `pulse-ring` traced around the square container; anything *with* one gets a new `pulse-ring-img` applied via `filter: drop-shadow()` directly on the `<img>` itself, which traces the rendered element's own alpha channel rather than its bounding box — so the ring follows the image's real (possibly non-square, transparent-cornered) visible shape instead of a mismatched square around it. Same expand-and-fade pulse feel as before, just via animated blur-radius instead of `box-shadow`'s spread-radius (`drop-shadow` has no spread parameter). Verified visually with a synthetic transparent-background star-shaped avatar: the star renders with no background square behind it in both the topbar pill and chat messages, and the green glow visibly hugs the star's points and inner concave notches rather than forming a square.
- **Ban list + unban, full mechanism (2026-07-11, same-day owner-reported gap — a ban with no way to reverse it isn't acceptable)**: `recordBan` also writes a parallel `session.bans` (`Map<banId, {ip, nickname, bannedAt}>`, opaque `banId` from `randomBytes(8)`) alongside the fast-lookup `bannedIps` `Set`. New creator-only WS cases `list-bans`/`unban` (same `isCreator` gate as kick/ban) round-trip through one shared `'ban-list'` reply message, consumed by `PeerManager.onBanList`. UI lives in `QuickRoomSettings.js`'s room-settings popover (`#quick-banned-section`, hidden entirely for non-creators) — `open()` calls `peerManager.listBans()` fresh every time the popover opens (not cached), and each row's Unban button just calls `unbanPeer(banId)` and re-renders from the next `'ban-list'` reply rather than optimistically splicing locally, so the shown list can never drift from server truth. (The two facts here that are still security-relevant standing rules — the raw IP is never returned to the client, and `nickname` is display-only/never trusted for authorization — stayed in CLAUDE.md's trimmed entry.)

### Additional entries moved 2026-07-24 (third housekeeping pass)

Same treatment as the two passes above — moved verbatim from CLAUDE.md's "Key conventions," each reduced there to a one-line pointer carrying only the still-load-bearing rule.

- **Room rail, full design + origin story (2026-07-24)**: supersedes a same-session lobby-only list-panel sidebar that shipped and was immediately replaced — the actual want was a slim icon-only rail, and one that persists during an active call for quick switching, a bigger scope than the list panel had. Final shape: `RoomRail.js`, mounted on **both** the lobby and the in-room page (`.room-rail`/`.room-rail-item` in `tailwind.css`), not a lobby-only panel. It builds its own DOM into `document.body` (same precedent as `SettingsPanel.js`'s `_buildModal()`), so neither `index.html` nor `lobby.html` carry rail markup — just one `.room-shell`/`.lobby-page` layout class reserving `var(--room-rail-width)` (4.25rem, zeroed via a single `:root` override below 767px, where the rail hides). Each room icon's corner status dot is still the same **live poll** as before, unchanged: `roomStatusPoll.js`'s `startRoomStatusPolling()` hits `POST /api/room-status` (`server.js`) every 25s, projecting `SessionManager.getSessionMeta()` down to `{active, peerCount, maxPeers}` — still poll-based, not push/WS presence, same deliberate scope limit as before (consistent with the prior rejection of a heavier contacts/presence system). Clicking a room icon uses the rail's **own** minimal join logic against `/api/validate-room` (a small popover anchored to the clicked icon handles password/full/error states) rather than `lobby.js`'s page-coupled `attemptJoin()`, since the rail has to work identically on a page with no join-form DOM at all (in-room). No unread/mention badge exists or is planned — infeasible without either new server-side chat persistence or a background presence connection to rooms not currently joined, both rejected as against this app's design.
- **Per-peer local block/mute, full narrative (2026-07-08)**: `UIController.blockedPeerIds` (`Set<peerId>`) is a local-only preference toggled from a new `.participant-block-btn` on each participant's card (`_buildBlockControl()`) — available to every peer, not gated behind moderator status like `_buildModeratorMenu()`, since it's a personal filter rather than a room-affecting action. `setBlocked(peerId, blocked)` is the single entry point for every side effect: audio is forced to 0 through the existing `_applyAudioVolume()` chokepoint regardless of the peer's saved volume slider value; video is suppressed by adding `!this.isBlocked(id)` to the `remoteIds`/`remoteStreams` filters in `buildGrid()`/`updateLayout()`, so a blocked peer's tile is never built and never auto-focused. Blocking a peer whose tile is currently watched also calls the existing `_unwatchTile()` — the **same** `onWatchChange` → `PeerManager.setWatched(streamKey, false)` → `unwatch-stream` signal already used for off-screen/LRU-evicted tiles, which makes the blocked peer's browser actually pause that `RTCRtpSender` (`sender.replaceTrack(null)`) on their end. Chat/reactions/typing/file-offers/polls from a blocked peer are separately suppressed in `PeerManager.js`'s data-channel message switch (`if (this.ui.isBlocked(peerId)) return;` ahead of each `this.ui.*()` call, or an auto-decline via `_respondToOffer(peerId, msg.fileId, false)` for file offers, mirroring the existing too-large-file auto-decline) — this had to happen there rather than inside `ChatUI`, because `ChatUI.addChatMessage()`/`showFileOffer()` only ever receive a pre-resolved nickname string, never a `peerId`.
- **Settings modal markup de-duplication, history (2026-07-17)**: it used to be hand-copied static HTML in both `index.html`/`lobby.html` (the old rule was "must mirror exactly," after the Voice Activity mic mode shipped in one copy but not the other). `public/index.html` dropped ~400 lines and `public/lobby.html` dropped ~263 in the commit that made this change (`e75c529`) — expect both files' line counts to look smaller than older references to them.
- **Shared emoji picker, full design (2026-07-11, meeting-recap workflow step 1)**: `EmojiPicker.js`'s `openEmojiPicker(anchorEl, onPick)` is the one entry point for both the message-reaction popover (`ChatUI.js`, replacing the old hardcoded 6-emoji `_emojiSet` strip) and the composer's "Add emoji" button (`App.js`, inserts at the textarea's caret via `insertAtCaret()`). Same singleton-popover pattern as `Tooltip.js` — one popover lazily built and reused, not rebuilt per open. Data (`public/assets/vendor/emoji-data.json`, trimmed from `emoji-picker-element-data@1.8.0`, Apache-2.0, 1914 emoji excluding skin-tone/hair "component" entries) is fetched lazily on the *first-ever* open, not at page load — zero cost for a session that never opens the picker, same as `VirtualBackground.js`/`NoiseSuppressor.js`. Category tabs scroll to a section rather than filtering; typing in search replaces the grid with a flat filtered list (substring match on name + tags). Each emoji button carries `data-tip="{name}"`, which rides on `Tooltip.js`'s existing delegated listeners for free — no extra wiring needed for the hover name. Recently-picked emoji persist to `localStorage['recentEmoji']` (personal preference, non-room-scoped, same treatment as `micThreshold`). **Scope cut, deliberate**: no skin-tone variant picker in v1.
- **Deafen hard-mutes the mic, keybind, auto-deafen-on-away, configurable away timeout, full mechanism (2026-07-14)**: `App.js`'s `toggleDeafen()`/`setDeafenUI()` are the single manual-toggle choke point (button click + a new global "Deafen keybind" set in Settings → Audio & Mic, captured the same click-then-press-a-key way as the mic keybind); the auto-deafen-on-away opt-in toggle piggybacks on the existing idle timer instead of a separate one. The idle timeout itself is now user-configurable (Settings → Profile's "Away after" dropdown, 5/10/15/30 min, `localStorage['awayTimeoutMinutes']`, read fresh via `getIdleTimeoutMs()` on every timer reset) rather than a hardcoded 15 minutes — also drives the plain (non-deafen) Away/`_reconcilePresenceStatus` transition, since both piggyback on one idle signal. The F11/F5/F12 keybind warning (`SettingsPanel._updateDeafenKeybindWarning`) was added after a real bug report — binding Deafen to F11 still toggled fullscreen.
- **Voice/sound resets the idle timer, mute-proof, full mechanism (2026-07-14, extended 2026-07-20)**: the idle timer (`App.js`'s `resetIdleTimer()`) only listens for `mousemove`/`mousedown`/`keydown`/`touchstart`/`scroll` on `document` — none of which fire while the tab is backgrounded/unfocused, so alt-tabbing away with no mic sound goes undetected as presence (this is an inherent limit of a DOM-input-only signal — see the 2026-07-14 owner report where talking mid-call while unfocused still triggered away/auto-deafen). A `setInterval(..., 500)` in `App.js` calls `peerManager.pollMicPresence()` and calls `resetIdleTimer()` whenever it reports `active`. **`pollMicPresence()`/`_rawMicLevel()` are deliberately separate from `pollSelfMicActivity()`/`_localMicLevel()`** (the speaking-ring/active-speaker signal) — the latter reads 0 whenever `micEnabled` is false, so a muted mic (the common case for anyone muting around background noise, e.g. a barking dog) went completely blind to this signal even though the original 2026-07-14 fix's own comment claimed otherwise. `pollMicPresence()` instead taps `_presenceMicStream` — a **cloned** copy of the raw mic track (`PeerManager._refreshPresenceMicStream()`, (re)built whenever `_rawMicStream` itself is replaced: `toggleMic()`'s first grant, `switchMicrophone()`'s device swap), whose `enabled` flag mute/deafen logic never touches. This can't just tap `_rawMicStream` directly: when noise suppression is off (the default), `_applyNoiseSuppression()` returns the raw stream unchanged, so `micStream` *is* `_rawMicStream` — the same `MediaStreamTrack` object — and `toggleMic()`/`setDeafened()`'s `track.enabled = false` would silence it too, since `enabled` is a property of the track itself, not per-consumer. `MediaStreamTrack.clone()` produces an independent track sharing the same hardware source, so the clone keeps hearing sound (a bark, talking to someone off-call) regardless of mute/deafen state. Polled at 500ms rather than a slower cadence since the analyser only samples an ~11ms instantaneous snapshot (`fftSize` 512) — a short bark has a real chance of landing between polls at a slower rate. Uses its own adaptive noise floor (`_presenceNoiseFloor`), not `_updateSpeakingStates`' per-peer state, since that fires `onSpeakingChange` and would light up the local speaking ring/trigger auto-focus from sound that was never actually transmitted. **Still-real limit**: goes blind only if the mic has never been enabled even once this session (no hardware stream to clone from) — DOM input events still cover that case; also, an away timer running with mic access never granted at all sees no benefit from this.
- **Room moderator — token-reclaim test gotcha, full detail (2026-07-05)**: re-presenting the creator token on a second connection (`claimModerator`) reassigns `session.creatorPeerId` to that *new* connection and demotes the original one out of both creator and moderator status — any test exercising the token-bypass path must switch to issuing further creator-privileged actions from the new socket, not the original.
- **Session recap export, full mechanism (2026-07-12, meeting-recap workflow step 3 — closes the workflow; Attendees/Polls sections added 2026-07-13)**: `UIController.exportSessionRecap()`, triggered by an always-visible `#export-recap-btn` in the Files tab (unlike the pre-existing `#files-download-all`/`_downloadAllFiles()`, which only appears once 2+ files are shared — a text-only recap of the conversation is still useful with zero files). Bundles one `recap.md` (room name/code + date header, an `## Attendees` list from `UIController.getAttendees()`, a `## Decisions` section from `ChatUI.getPinnedMessages()`, a `## Polls` section from `ChatUI.getPollSummaries()`, a `## Chat transcript` section from `ChatUI.getMessageHistory()` — the latter three are read-only accessors over `_pinned`/`_polls`/`_messageMeta`) plus every shared file under a `files/` zip subfolder (kept separate from the root so a shared file literally named `recap.md` can't collide). Reuses the exact JSZip instance/global + blob-download mechanics `_downloadAllFiles()` already had — the file-de-duplication-naming loop and the generate-blob-and-`<a download>` dance were extracted into shared `_addFilesToZip()`/`_downloadZip()` helpers so neither export path duplicates the other's logic. **Not moderator-gated** — unlike pinning, this only ever exports what the local peer has personally already seen (their own rendered chat + received files), never another peer's private state, so it's symmetric across every participant, same trust level as a local screenshot. `UIController._attendees` (every nickname seen this session, self + any peer who ever sent a real nickname) is deliberately never pruned on leave — unlike `peerVolumes`/`blockedPeerIds` it must survive a peer leaving, and unlike those it's also **not** reset in `clearAllParticipants()`, since an `'init'` there is a same-session reconnect (fresh peerIds), not a new room. `ChatUI.getPollSummaries()` resolves each vote's voter peerId back to a nickname via the existing `getNickname` constructor dep, with the same shortened-peerId fallback as everywhere else if that voter has since left. Verified via a scripted Playwright pass that captures the real download and unzips it *inside the page* using the app's own already-loaded `JSZip` global (`page.evaluate`) rather than adding a Node-side unzip devDependency just for the check.
- **Caption+files grouped messages, full mechanism (2026-07-09)**: sending 1+ staged files (with or without a typed caption) renders as one Discord-style bubble — one avatar/sender/timestamp header, the caption (if any), then a slot per file — instead of a caption chat message immediately followed by separate per-file messages. `App.js`'s `sendMessage()` calls `PeerManager.sendFilesWithCaption(files, caption, replyTo, messageId)`, which mints one `groupId` (batch id) and still sends each file through the existing, unchanged `sendFileToAll(file, group)` → `_sendFileToPeer` per-peer offer/accept/transfer pipeline — **grouping is a rendering concern layered on top, not a transport change**, so one AFK peer or one blocked/oversized file still can never hold up any other file in the batch. `group` (`{ groupId, caption, replyTo, messageId }`) rides in every `file-offer` payload (duplicated per file rather than sent once, so the group/caption reconstructs correctly even if an earlier file in the batch gets bounced or declined first — no ordering assumption needed). `ChatUI.ensureFileGroup(sender, groupInfo)` is idempotent per `groupId` and creates the shared bubble (only the *first* file-offer or the sender's own first file actually builds it); `ChatUI._fileSlot(groupId, fileId)` lazily creates each file's slot inside it, with **two independent sub-areas** — `.chat-file-slot-content` (offer prompt → cleared on accept/decline → final file card) and `.chat-file-slot-progress` (transient upload/download bar) — specifically so a progress-bar update can never clobber a content area that, for the sender's own instant local echo, may already hold the finished file card. `showFileOffer`/`updateFileProgress`/`removeFileProgress`/`addFileMessage` all now take a trailing `groupId` and write into a slot instead of appending their own standalone chat-log children. Reactions/reply/pin/hover actions attach to **every** group with a `messageId` — caption or not (scope cut lifted 2026-07-15: `ensureFileGroup` now records `_messageMeta` and calls `_setupEmojiPicker` whenever `messageId` exists; Copy/Edit are gated on non-empty text inside `_setupEmojiPicker` itself, Delete works for any own group, reply-quotes and pin snapshots of a files-only group fall back to the first filename); the caption also notifies (`_playMessageSound`/unread dot) immediately like a normal chat message, while a files-only group keeps the old file-offer behavior of staying silent until a transfer actually completes. **Incidental fix while restructuring this**: a blocked peer's `isBlocked()` check now runs *before* anything (including the ensureFileGroup call) for `file-offer` handling, not after the allow-list/size checks — previously a blocked peer sending a disallowed-extension or oversized file could still leak a "Blocked incoming file: X" system message before the block check was reached. `addChatMessage()`'s reply-quote HTML/wiring, code-block-highlight/copy-button/link-preview post-processing, and "only auto-scroll if already near the bottom" logic were extracted into shared `_replyQuoteHtml()`/`_wireReplyQuote()`/`_finalizeMarkdownBody()`/`_scrollIfAtBottom()` helpers so the group's caption reuses the exact same treatment. Guarded by `tests/caption-file-group.mjs` (`npm run test:caption-file-group`).
- **Voice Activity mic mode + adjustable mic sensitivity, full mechanism (2026-07-06)**: a 4th `micMode` (`index.html`'s `#settings-mic-mode-picker`) alongside Toggle/Push-to-Talk/Push-to-Mute — only actually transmits while the speaking-detection signal (`PeerManager._updateSpeakingStates`, see the active-speaker note below) says you're currently speaking, instead of the mic being live the whole time it's toggled on. The detection margin itself is now user-adjustable (`localStorage['micThreshold']`, Settings' "Mic sensitivity" slider, shown only in this mode) rather than the hardcoded `0.03` — note this one constant also drives detection for every peer as computed locally on your own client, so it subtly affects how sensitive *other* people's speaking-ring looks to you too; that's a harmless local-only side effect, not a broadcast change.
  - **Gating mechanism**: mic audio now goes through `PeerManager._addTrackedStream(pc, peerId, micStream, 'mic')` (previously a bare `pc.addTrack`) so each connection's mic `RTCRtpSender` is recorded at `senders[peerId]['mic-audio']`, the same map the video watch/unwatch pause system uses. `PeerManager._reconcileMicGate(speakingLocally)`, called every 200ms from `_pollActiveSpeaker()`, does `sender.replaceTrack(shouldTransmit ? liveTrack : null)` per connection — **not** `track.enabled = false`. That distinction matters: `_localMicLevel()`'s analyser taps the *same* `MediaStreamTrack` used for transmission, so disabling the track itself would also blind the analyser and the gate could never reopen once closed. `replaceTrack` only pauses the outbound `RTCRtpSender`, leaving the source track (and the analyser reading it) untouched.
  - Reconciliation runs unconditionally every tick rather than only on a state change, specifically so a sender added mid-gate (a new peer joining while the gate happens to be closed) gets brought in line on the very next tick instead of transmitting live until the next actual speaking transition. Outside voice-activity mode (or while manually muted via the mic-toggle button), it always resolves to the live track — this is what guarantees switching mic modes, or unmuting, can never leave a sender stuck gated closed from a prior voice-activity session.
  - Tested in `tests/mic-gate-logic.mjs` (`npm run test:mic-gate`) as pure logic against a mocked `senders`/`micStream`, same style as `tests/active-speaker-logic.mjs` — real fake-audio-device detection in a headless browser is too unreliable to assert on directly (see the active-speaker note's existing testing-limitation callout).
  - **Live mic-level meter for tuning (2026-07-06)**: Settings' "Mic level" bar (`#mic-meter-row`, shown alongside the sensitivity slider, voice-activity mode only) gives visual feedback while calibrating — a moving level bar, a vertical line marking the current `micThreshold`, and the bar turns green exactly when that level would currently count as speech. `PeerManager.pollSelfMicActivity()` is the public entry point this reads from every 100ms (`SettingsPanel._startMicMeter`/`_stopMicMeter`, started in `open()`/stopped in `close()`) — it deliberately does **not** rely on `_pollActiveSpeaker`'s regular 200ms loop, because that loop only starts once there's at least one peer connection (`createPeerConnection`'s `Object.keys(this.peers).length === 0` check). Testing/tuning sensitivity solo, before anyone else has joined, needs its own call into the same real `_updateSpeakingStates` function — reusing the actual production algorithm rather than a separate approximation, so the meter always matches what would really happen once a call is live. Running both loops concurrently during an actual call (Settings open while peers are connected) is harmless — same deterministic math over the same signal, just sampled at a different cadence.
- **Bandwidth-saving watch tracking — the two 2026-07-08 "they can't hear me" bugs, full detail**: this applies to *detection* too, not just removal: `receiveOffer()` used a live-track-kind check to decide whether the mic was already added, missed the gated-closed mic sender, and re-added it — creating a never-negotiated duplicate that overwrote `senders[from]['mic-audio']` and stranded the real sender at `replaceTrack(null)` (one of the two 2026-07-08 "they can't hear me" bugs; the other was `removePeerStream()` looping `pc.getSenders()`/`removeTrack()` — our *own outbound* tracks — on a remote peer's `stop-sharing`). Both guarded by `tests/audio-sender-lifecycle.mjs`.
- **Mobile camera facingMode self-heal, the cache-poisoning bug it fixed, full detail (2026-08-10)**: the first version of this fix (still self-consistent, but had a real bug caught in testing) cached each candidate under the *requested* direction — so when the fallback probe landed on the front lens while trying to satisfy an 'environment' request (the only other lens that actually streamed), it permanently poisoned `camDeviceId:environment` with the front camera's id, and separately `toggleCam()` hardcoded `this._camFacingMode = 'user'` even when opening via an explicit rear `camDeviceId`, mislabeling state from the very first open. Fixed by verifying every candidate's *actual* direction via `track.getSettings().facingMode` before accepting it — a mismatch (stale/poisoned remembered id, or facingMode honoring the wrong lens) is re-cached under its *true* direction and the search continues rather than being accepted, which self-heals an already-poisoned cache within one flip.

### Additional entries moved 2026-09-02 (fourth housekeeping pass)

Same treatment as the three passes above — moved verbatim from CLAUDE.md's "Key conventions," each reduced there to a trimmed standing rule (kept) plus a pointer to this section. This pass targeted the longest entries (by raw character count) that had accumulated into full incident narratives with no further forward-facing rule beyond what's now in the trimmed CLAUDE.md version.

- **Chat auto-scroll, full rework narrative (2026-08-31, owner-reported: reacting to the last visible message didn't scroll it into view, and reduced-motion/panel-resize made it feel unresponsive)**: `addReaction()` now calls `_scrollIfAtBottom()` after repainting the badge bar, same as a new message — previously reacting to the bottom-most message left the badge below the fold with nothing scrolling it into view. A bulk chat-history load (`UIController._loadChatHistory`) used to call the smooth per-message scroll once for every historical entry in a tight synchronous loop — each call re-targeting/restarting the previous still-in-flight animation — and appended the "Today" divider *after* that loop with no follow-up scroll, so it could hang just out of view; `addChatMessage()`'s `isHistorical` branch no longer scrolls at all, and `_loadChatHistory` does one instant `chatLog.scrollTop = chatLog.scrollHeight` jump after the whole batch (divider included) is in the DOM. A `ResizeObserver` on `#chat-log` (`ChatUI._wireAutoScrollResize()`, wired once from the constructor) re-pins to bottom whenever the log's box resizes — a chat-panel drag-resize or a window resize both reflow message text (word-wrap changes) and shift `scrollHeight` without ever firing a `'scroll'` event, so a user reading live traffic at the bottom would otherwise silently drift away from it.
- **Draggable self-view PiPs — corner-relative positioning, full bug narrative (2026-09-02, owner-reported: "i like to place mine in the bottom right but right now when i resize the window they stay relative to top left")**: the persisted `pipPosition:<role>` shape changed from `{left, top}` (always measured from the top-left corner) to `{x, y, anchorRight, anchorBottom}` — an offset from whichever corner the PiP's center was nearest to at drop time. `_makeDraggable()`'s new `pinToCorner(rect)` (called from `pointerup` once a real drag — not a click — is detected) decides the corner and writes `right`/`bottom` CSS instead of `left`/`top` when nearer that edge; during the drag itself, cursor-tracking still uses plain `left`/`top` (simplest math for "follow the pointer"), the corner is only decided once, from the final dropped position. `_reclampDraggedPip()` (still fired on `window`'s `resize` listener, and from `setEnlarged()`'s post-transition reclamp) now only has to clamp the stored offset back on-screen if the window shrinks smaller than it, reading which corner via `el.dataset.pipAnchorRight`/`pipAnchorBottom` rather than re-deriving it. No migration for already-saved old-format positions — they simply don't restore (falls back to the default right-edge auto-stack) and get re-pinned correctly on the next manual drag, a one-time reset judged not worth extra migration code for a purely cosmetic, per-viewer preference.
- **Self-view unified into grid/focus, full mechanism (2026-08-29, owner-reported: wanted to see their own stream fullscreen, or alongside peers in the grid, plus a persisted way to hide it entirely)**: `buildGrid()` appends `['me','me-cam'].filter(id => this.streams[id])` after the remote tiles (skipped entirely if "Hide my own preview" is on) — self is always rendered as if "watched" (`_buildGridCell`'s `isWatched = isSelf || this.watchedTiles.has(peerId)`) since a local stream has no `RTCRtpSender` to pause for bandwidth, and gets no "stop watching" button for the same reason. Clicking a self tile calls the same `focusStream(peerId)` every peer tile already uses — nothing peer-specific there, it just looks up `this.streams[peerId]`. Two lookups needed translating the literal `'me'`/`'me-cam'` keys to the real `this.selfPeerId` to resolve correctly: `_peerNickname()`/`_updateGridMicIcon()` both index by real peerId (`#participant-<peerId>` cards, `_micEnabled[<peerId>]`), so `_buildGridCell()`/`_updateFocusMeta()` resolve `actualPeerId = this.selfPeerId` for self and append " (you)" to the nickname, and `_updateGridMicIcon()` additionally checks `'me'/'me-cam'` whenever `peerId === this.selfPeerId` so muting yourself live-updates your own grid tile (previously only the participant-card dot, self was never a grid tile at all). `_updateSelfViewPositions()`'s right-edge auto-stacking now checks actual visibility (`style.display !== 'none'`) too, so a hidden PiP doesn't leave a reserved gap next to the one still showing. `removeStream()`'s self branch no longer returns early before touching `focusedPeerId` — turning off your camera/screen while focused on it (now possible, previously self was never focusable) needs the same re-focus logic peer removal already had, generalized to prefer a remaining remote peer over your other self stream (e.g. losing focus on your screen share while your camera is still on shouldn't jump to your own camera if a peer is right there).
- **Self-view click-to-enlarge, full mechanism (2026-08-29, owner-reported: fixed 150px PiP too small to actually judge what a peer sees of your own camera/screen)**: `_makeDraggable()`'s pointerdown/pointermove/pointerup already existed for drag — now also tracks total pointer movement (`moved`, >4px threshold) so a plain click/tap (no meaningful movement) toggles `.self-view-pip-large` instead of being swallowed as a zero-distance drag. Growing/shrinking re-clamps a manually-dragged PiP via the existing `_reclampDraggedPip()` so enlarging near a screen edge can't push it out of view. Tooltip (`data-tip`) flips between "Click to enlarge"/"Click to shrink" to make the interaction discoverable.
- **Webcam quality/fps live-apply, the hardware reproduction story (2026-08-29)**: picking a new resolution/fps in either webcam picker used to require toggling the camera off and back on to actually take effect — `applyCamQualitySettings()` only ever called `track.applyConstraints()`, which silently no-ops on a lot of real UVC webcam hardware (confirmed against a real Logitech C922) even though the call resolves without throwing, since many drivers can't renegotiate capture format mid-stream. A second `getUserMedia()` against a still-open device silently reuses whatever format the first session already negotiated instead of renegotiating (reproduced both against the real C922 and against Chromium's own fake test device) — which is why the fix has to stop the old hardware track first, forcing a real renegotiation, rather than acquiring-then-stopping the way `switchCamera()` does for a genuinely different capture session.
- **Discord-style grouped dock buttons, the redesign narrative (2026-08-29, owner-reported: "i need the buttons in the control bar to be grouped buttons... more like discords ones... clear what belongings to what cause of the grouping outlines")**: `.dock-btn-wrap` (Share, Camera, Mic, Deafen, Record — each holds a main action plus its quality/options `.dock-caret`, some also an alternate button like Switch Camera) now renders as one bordered/backgrounded "chip" (`border`, `border-radius: 0.75rem`, `background: var(--bg1)`) instead of the caret being an easy-to-miss 1rem corner badge floating over an unmarked icon. `.dock-caret` changed from `position: absolute` (a tiny badge overlapping the main button) to a proper inline flex segment, full height, separated by its own `border-left` — a real Discord-style split button, not a decoration.
- **Webcam resolution picker capability-detection, full mechanism (2026-08-29)**: the "Webcam resolution" segmented picker (Settings' Screen & Video section *and* its `QuickRoomSettings.js` duplicate) used to hardcode 360p/480p/720p/Source regardless of what the actual camera could do — a 1080p-capable webcam (e.g. a Logitech C922) had no way to be selected at anything above 720p except by choosing the uncapped "Source" option. `PeerManager._recordCamCapabilities(track)` reads the real ceiling off a just-opened camera track via `track.getCapabilities()` (`width.max`/`height.max`, supported by Chrome/Edge/Firefox), stores it on `this.camCapabilities` and persists it to a flat `localStorage['camCapabilities']` key (same "remember the last one" tier as `camDeviceId`) so the picker has something to show even before the camera's been turned on again this session — called from both `toggleCam()` and `switchCamera()` (a device switch can mean a different physical camera with a different ceiling). `SettingsPanel`/`QuickRoomSettings` each keep their own copy of the candidate-list-filtering logic (second UI surface, not a second data model) — `SettingsPanel` also live-refreshes its picker via `peerManager.onCamCapabilities` if Settings happens to already be open when the camera is first turned on; `QuickRoomSettings` doesn't hook that same callback (would collide with SettingsPanel's assignment on that one field) and instead just rebuilds fresh on every popover `open()`.
- **Resume mic/cam/screen-share after a refresh, full reasoning (2026-08-21)**: `App.js`'s `pagehide` listener saves the current state every time the page is actually about to go away (refresh, tab close, navigating off) — not just at the moment of the last manual toggle — so the 5-minute TTL window measures genuine gone-time, not "how long since you last clicked the mic button" (someone on a long call with mic untouched would otherwise find their state already "expired" on refresh). The per-toggle `saveMediaState()` calls at each mic/cam/share click handler (and `onForceStopped`) are a secondary safety net for the rare case `pagehide` doesn't fire. Mic/cam restore silently via `getUserMedia()`, which (unlike `getDisplayMedia()`) doesn't require a fresh user gesture once permission's already granted to the origin — restoring mic also checks `peerManager.micPolicy !== 'ptt'` first, since PTT has no persistent "mic on" state to restore. `pagehide`'s save is skipped whenever `leavingRoom` is already true, so it can't resurrect what `leaveSession()` just cleared.
- **Pinned messages, full mechanism (2026-07-12, meeting-recap workflow step 2)**: session-lifetime, low-stakes/cosmetic — every peer already tracks moderator status locally (`UIController.moderatorPeerIds`, synced via the existing `'moderator-update'`/`'init'` handling) for the crown badge, so pinning didn't need its own server round-trip. `ChatUI.js`'s `applyPin(messageId, pinned)` is the single local-apply entry point, called both for the local moderator's own action (via a bookmark-icon Pin/Unpin button in the hover action bar, rendered only when `this._isModerator()`) and for a remote peer's already-authorized pin. Pin state (`ChatUI._pinned`, messageId → `{sender, rawText, pinnedAt}`, capped 50) is a content **snapshot**, deliberately independent of both `_messageMeta`'s 300-cap and the DOM log's 100-message `maxMessages` cap — a pin is meant to survive normal chat churn that would otherwise prune it. `applyChatDelete` also removes a deleted message from `_pinned`. The `#pinned-messages-btn` (chat panel tab bar) + `#pinned-messages-panel` popover (click-to-scroll into the original via the same `scrollIntoView`+`.chat-message-highlight` snippet `_wireReplyQuote()` already uses) are wired entirely inside `ChatUI.js`'s own constructor (`_wirePinnedPanel()`) — no App.js involvement needed, same self-contained-popover pattern as `EmojiPicker.js`/`Tooltip.js`.
- **Tab-unfocused vs. actually-away presence, the "why" (2026-07-14)**: previously both `handleTabVisibility()` (tab switched away/backgrounded) and `handleIdleChange()` (15-min mouse/keyboard/touch inactivity) fed one shared `_reconcileAwayStatus(away)` and both produced the same yellow 'away' — so alt-tabbing to check something while still at your desk looked identical to genuinely stepping away, which undersells presence info for someone using a room all day like a persistent Discord call. `PeerManager` tracks `_tabHidden`/`_isIdle` as two independent booleans (previously the boolean was passed straight through, never stored). No new message type or receive-side allowlist was needed — `status-update`'s payload was never validated against a fixed enum, so a peer sending `'unfocused'` just works through the existing `updateParticipantStatus` path.
- **Controls bar toggle — the redesign narrative across three same-day passes (2026-08-29/2026-09-02)**: `#controls` used to be hover-to-reveal (`body:hover #controls`/`body:focus-within #controls { display: flex; }`) — replaced with an explicit open/close toggle so it behaves like the chat/members side panels instead of twitching open on any mouse movement. **Flush-against-the-side-panels, two passes the same day (owner-reported, screenshot-driven)**: once always-visible, the dock read as "a separate floating card dropped onto the stage" rather than a connected bottom bar. First pass squared `#controls.controls-bar`'s corners (`border-radius: 0`, was rounded) and matched its background to the panels' own `--bg2` (was `--bg3`, `.dock-bar`'s normal "elevated floating pill" tone) — a real improvement, but the owner correctly called it out as incomplete: a genuine layout gap remained, caused by `.chat-resize-handle`/`.members-resize-handle` living *outside* `#videos` as their own flex column (see the "Resize handles live inside #videos" entry for the actual structural fix) — the first-pass diagnosis blamed the transparent handles' color rather than the space they were reserving, which was the real cause. Left here as a reminder that a color/radius fix can visually improve a layout bug without addressing its root cause, and it's worth checking for a structural explanation before assuming a styling tweak alone closed something out. **In-flow, not an overlay (2026-09-02, owner-reported: in grid view the dock visually covered the bottom row of tiles)**: `#controls.controls-bar` was still `position: absolute` over `#videos` this whole time — none of the passes above changed that, they only fixed how it looked. Changed to a normal (non-growing) flex child of `#videos`' own flex column, same treatment as `#stage-header`. `position: relative` is kept (not dropped) since `.controls-close-btn`'s handle is `position: absolute; top: -1.75rem` and needs a positioned ancestor to anchor against. Going in-flow surfaced a second bug: `#videos`' `justify-content: center` centers its flex items as a group whenever nothing else is growing, which left the dock vertically centered instead of pinned to the bottom — fixed with `margin-top: auto` on `#controls.controls-bar` (a no-op once `#grid-view`/`#focused-view` ARE flex-growing, since there's no leftover space left for it to claim; this exact interaction is what caused the 2026-09-02 spinner off-center-then-invisible bug — see CHANGELOG.md's "Visual" section). **Close handle hover-revealed (2026-09-02, owner-reported)**: `.controls-close-btn` used to be always visible, permanently poking up above the dock. Now `opacity: 0; pointer-events: none` at rest, faded in via `#controls:hover`/`#controls:focus-within` (the latter for keyboard users tabbing into the dock).
- **Resize handles live inside `#videos`, the bug narrative (2026-08-29, owner-reported)**: `#chat-resize-handle`/`#members-resize-handle` used to be their own flex items between `#chat`/`#members-sidebar` and `#videos` in the room-shell's flex row — a dedicated 4px-wide layout column that left `#videos`'s actual edge (and anything positioned relative to it, like `#controls` or the panel tabs) sitting 4px short of the panel it was supposed to be flush against. This was worked around for the panel tabs (`.chat-tab`/`.members-tab` used to pull back `-0.25rem` to overlap the handle, needing `#videos { z-index: 61 }` so the tab would still win the click in that strip) but never fixed for `#controls`, which is what surfaced it. Fixed at the root: both handles are now `position: absolute` children of `#videos` itself (`top:0;bottom:0;left:0` / `right:0`, full height, no negative offset needed since `#videos`'s edge is now genuinely the panel's edge). The tabs' pull-back hack is gone too (`left:0`/`right:0` now, no offset) — a tab and its handle are never visible at the same time (the handle hides exactly when its panel does, exactly when the tab shows), so there's no overlap left needing `z-index` arbitration; `#videos`'s `z-index:61` is kept anyway as a general stacking guarantee, not because anything still specifically needs it.

### Additional entries moved 2026-09-02, second pass (fifth housekeeping pass)

Same treatment as the passes above — moved verbatim from CLAUDE.md's "Key conventions," each reduced there to a trimmed rule + pointer. This pass specifically left every security/authorization statement (server-enforced authority layers, consent trust boundaries, receive-side ownership checks) intact in CLAUDE.md rather than moving them — only the surrounding narrative/UI-mechanic prose moved here. The Room moderator entry's sub-bullets and the Custom avatars entry were reviewed and deliberately left untouched in CLAUDE.md entirely (every sub-bullet there either states or supports an authorization invariant).

- **Peer-connecting card state + join chime, full narrative (2026-08-26)**: the "X joined" sound used to fire straight off the WS `peer-joined` signaling event (`PeerManager.js`'s `peer-joined` case → `UIController.addPeer()`) — pure signaling, before any `RTCPeerConnection` had finished ICE/DTLS, so it could sound (and by extension imply "they can hear you now") well before any audio could actually flow either direction. `UIController.setPeerConnecting(peerId, connecting)` now dims a just-created card (`.participant-connecting`, tailwind.css — reduced opacity + a pulsing grey status dot, distinct from any real presence color) the instant it's created — from both `addParticipant()` (peers already in the room at `'init'`) and `addPeer()` (someone joining after you) — and `PeerManager.js`'s `pc.onconnectionstatechange` calls `UIController.markPeerConnected(peerId)` once `connectionState` actually reaches `'connected'`, which clears the dimming and (only for the `addPeer()` path, tracked via a `_pendingJoinSounds` Set so pre-existing room members never replay the chime once your mesh connection to each of them comes up) plays the join sound.
- **Raise hand + raised-hands queue, full mechanism (2026-07-15)**: `#raise-hand-toggle` (dock button, amber `.dock-btn-active-amber` fill while up) → `PeerManager.setHandRaised()` → a `hand-status` WS broadcast (same fall-through list as `mic-status`/`deafen-status` in `WebSocketServer.js`, re-broadcast on `init`+`peer-joined` for late-joiner sync). `UIController.updateParticipantHand(peerId, raised)` is the single receive-side entry point: amber `front_hand` badge on the card's `.participant-status-icons`, plus the `#raised-hands-section` queue pinned below `#participants` (hidden while empty via inline `style.display` — unlayered-class-beats-`.hidden` standing rule).
- **Live room password rotation, full mechanism (2026-08-25)**: the creator can change (or remove) the room's join password mid-call from `QuickRoomSettings.js`'s "Room password" field. `SessionManager.setPassword`'s doc comment records the "deliberately only affects future joins" tradeoff explicitly, in case it's ever revisited as a real design decision rather than a silent behavior change. Because current peers stay connected, they need the *actual new password* pushed to them too — not just a `hasPassword` flag — so their own state doesn't go stale: `App.js`'s `onPasswordUpdate` callback updates the module-level `roomPassword` variable (not just `sessionStorage['roomPassword']` — `connect()`'s reconnect path reads the variable, so updating only storage would let a later server-restart reconnect retry with the stale password and bounce into the password prompt), `updateSavedRoomPassword()` (`savedRooms.js`) keeps a matching saved-room entry from going stale too, and `ui.setHasPassword()` live-updates the topbar lock icon without re-triggering `setRoomMeta()`'s chat-history-load logic. The Invite popover's "Invite by email" button is available to every peer, not just the creator, so keeping their copy current is what keeps that button correct after a rotation.
- **Live room topic banner, full mechanism (2026-08-26)**: faithful structural copy of the room-password rotation pattern, for a short creator-settable session-lifetime topic line shown in `#room-topic-banner` under the top bar. Settable at room creation too (`lobby.html`'s `#create-topic` field → `/api/create-room`'s `topic` body field, same normalization tier as `name`). Without the resend-on-join behavior, the room's only peer having their socket drop and reconnect (or even just refreshing the page) while nobody else is in the room deletes-then-lazily-recreates the session via `SessionManager.removePeer()`'s immediate-delete-when-empty behavior, which would otherwise silently lose the topic; `SessionManager.addPeer()`'s lazy-create path adopts the resent `topic` the exact same way it already adopts a resent `password`. `UIController.setTopic()` is the single live-update chokepoint (mirrors `setHasPassword()`'s "touches only its own DOM, no side effects" shape) — also derives the creator-only edit-pencil visibility and shows the banner (with just the pencil, no text) to the creator even when no topic is set yet, so there's a way to add one; re-derived from `updateModeratorStatus()` too, since creator status can change.
- **Local call recording, full mechanism (2026-08-26)**: a dock "Record" button + mode-picker popover (`#record-options-popover`, the standard `wireQuickPopover()`/`wireSegmentedPicker()` pattern). `PeerManager.isRecordingAllowed(peerId)` defaults to allowed until a peer explicitly opts out. While an 'audio'/'composite' recording is active, `recording-status` broadcasts a card badge (`.participant-recording-icon`, `UIController.setPeerRecording()`) + inline system message to every other peer — mirroring the existing "everyone sees who has moderator powers" transparency stance; 'local' mode never broadcasts this since it captures nobody else. Not moderator-gated — any peer can record, same trust tier as the existing session-recap export. `CallRecorder.stop()` returns a Promise that only resolves once the file has actually been handed to the browser as a download; `App.js`'s `leaveSession()` awaits it so navigating away mid-recording doesn't cut the file off.
- **@mention autocomplete, full design (2026-08-31, `MentionAutocomplete.js`, owner-reported)**: since `_processMentions()` only ever highlights an `@` immediately followed by one of the room's *exact* current nicknames, typing one by hand (nicknames can contain spaces/unicode and up to 60 chars) was error-prone with no help — this closes that gap with a Discord-style popover anchored at the live caret, not a button-triggered menu (the owner explicitly didn't want another "click first" step like the composer's "+" emoji menu). Query detection (`findMentionQuery()`) looks for an `@` that starts the text or follows whitespace, then treats everything up to the caret as the live query — deliberately *not* stopped at the next space, since a query needs to be able to contain spaces itself to match a multi-word nickname; it only closes once nothing in the participant list matches, or on a newline/length cap. Caret pixel position has no textarea API, so `composerCaret.js`'s `caretRect()` uses the standard hidden "mirror div" technique (a styled off-screen div holding the pre-caret text plus a marker span) — `MIRRORED_PROPS` only copies properties that affect text layout/wrapping, since the mirror is never actually shown. Picking a suggestion (`pick()`) splices in `@name ` and dispatches a real `input` event so `App.js`'s own auto-grow/typing-status listener reacts exactly like a manual edit would, guarded by a one-shot `suppressNextInput` flag so that synthetic event doesn't immediately re-open the popover (the inserted text can itself still substring-match a longer nickname).
- **Autocomplete hover bug, full narrative (2026-08-31 fix, owner-reported: "I can hover items but can't click them, have to use keyboard Enter to select")**: both `MentionAutocomplete.js` and `EmojiAutocomplete.js` originally called their `renderList()` (which does `listEl.innerHTML = ''` then recreates every item) from each item's own `mouseenter` handler, just to toggle which one had the `.active` class. Replacing the DOM node currently under the mouse pointer — even with a visually identical replacement at the same screen position, with the mouse not having moved at all — makes the browser re-hit-test and fire a **new** `mouseenter` on the replacement node, which called `renderList()` again, which replaced it again... a self-sustaining loop that kept swapping the hovered item out from under the cursor before a real click's mousedown/mouseup pair could ever land on one stable element (reproduced via Playwright as "element was detached from the DOM, retrying" on every click attempt). Fixed by splitting the two concerns: `renderList()` now only ever runs when the **match set itself** changes (from `open()`, after a `refresh()`/`applyMatches()`), and a new `setActive(i)` — a plain `classList.remove('active')`/`add('active')` toggle against an `itemEls` array kept in sync by `renderList()` — is what hover (`mouseenter`) and keyboard arrow-nav now call instead.
- **:emoji autocomplete, full design (2026-08-31, `EmojiAutocomplete.js`, companion to the entry above)**: same caret-anchored-popover shape as `@mention` autocomplete (shares its caret math via `composerCaret.js`), triggered by `:` instead of `@`. **No canonical `:smile:`-style shortcode table exists in this app** — `EmojiPicker.js`'s vendored dataset only carries a plain-English `n` annotation ("grinning face") plus a `t` tags array, not gemoji-style short names — so rather than vendor a second dataset just for exact names, `findMatches()` fuzzy-matches the query against that same annotation/tags data (identical matching to `EmojiPicker.js`'s own search box). Shares `EmojiPicker.js`'s lazily-loaded dataset via its re-exported `ensureEmojiData()`/`getRecentEmoji()`/`recordRecentEmoji()` (added to that file the same day) rather than fetching/caching a second independent copy — picking a suggestion also calls `recordRecentEmoji()` so recency stays in sync with the click-to-open picker. Before the dataset has ever loaded (first `:` typed on a fresh page load, if the click-to-open picker hasn't been opened first), the popover briefly shows a "Loading…" placeholder — `loadGeneration` guards that in-flight load against a later keystroke (or dismissal) superseding it before it resolves. The two triggers are wired independently and don't coordinate with each other — each only acts while its own popover is open, and each trigger character's own "must follow whitespace" guard means a realistic overlap (e.g. typing a mention nickname that itself contains a colon) essentially never produces two popovers open at once in practice; this wasn't engineered around further since the worst case is a rare, purely cosmetic double-popover, not a functional break.
- **Message edit/delete, full UI mechanism (2026-07-11)**: `ChatUI._messageMeta` (`Map<messageId, {isSelf, rawText}>`, capped at 300) powers the hover-bar Edit/Delete buttons (only rendered when `isSelf`), the inline edit textarea (`_beginEdit`: Enter saves, Escape cancels), and the Copy button (which reads current `rawText` from the map, not its stale closure param). `applyChatEdit()` appends a `.chat-edited-tag` "(edited)" after the timestamp; `applyChatDelete()` removes the bubble outright — no tombstone, and reply-quotes keep their snapshot text (their click-to-scroll just no-ops). Delete uses a two-step armed confirm on the button itself (tooltip flips via `data-tip`, reverts after 3s) — same pattern as Settings' clear-data.
- **Clear all local data, full UI/confirm-pattern detail (2026-07-06)**: `#settings-clear-data` in Settings' Privacy & P2P section (present in both `index.html` and `lobby.html`'s separate copy of the settings markup, wired once in the shared `SettingsPanel._wirePrivacy()`). No confirm-before-destructive-action pattern existed anywhere else in the app to reuse (removing a saved room and kicking a peer both fire immediately, no confirmation) — this introduces a two-step "click again to confirm" button (reverts after 4s if not confirmed) rather than a native `confirm()`, to stay in the app's own visual language. Worth knowing if you're the one testing it: this also wipes `nickname` and `peek.savedRooms` (including saved rooms' plaintext passwords), not just cosmetic prefs — `App.js` regenerates a fresh random nickname on the reload that follows, it does not come back empty.
- **Local chat history retention, mechanism detail (2026-07-24, opt-in)**: `ChatUI.js`'s `addChatMessage()`/`ensureFileGroup()` (caption text only, never file entries) persist via `appendMessage()`, `applyChatEdit()`/`applyChatDelete()` call `updateMessage()`/`deleteMessage()` so persisted history can't drift from what's actually still true. Loaded history renders through the same `addChatMessage()` path as live messages but with `isHistorical=true`: muted (`.chat-message-history`), no hover action bar (Edit/Delete/reply/emoji-picker — interacting with a days-old message as if live means nothing once the original peers are gone), and a one-time `.chat-history-divider` marks where live traffic starts. Reactions aren't persisted or restored; no per-room enable/disable, one global toggle like `micThreshold`/`autoAcceptFiles`.

### Additional entries moved 2026-09-02, third pass (sixth housekeeping pass)

Same treatment as the passes above — moved verbatim from CLAUDE.md's "Key conventions," each reduced there to a trimmed rule + pointer. This pass again preserved every XSS-safety/trust-boundary statement (DOM-based mention wrapping, textContent-not-innerHTML for the file viewer, the already-validated-avatar-data-URL interpolation reasoning) verbatim in CLAUDE.md, moving only the surrounding mechanism/UI narrative here. The remaining longest entries after this pass (Server-side abuse limits, File-transfer trust boundary/consent, Offline-first, Custom avatars, Room moderator) are all explicit security/authorization rule paragraphs and were deliberately left untouched again.

- **Combo keybinds capture-flow detail (2026-08-21)**: `SettingsPanel._wireKeybindCapture()` no longer finalizes on the first keydown — a modifier keydown only updates the live "recording" display and keeps listening; the first *non-modifier* keydown finalizes the capture (Escape with no modifiers held cancels instead). Modifier state itself is read straight off the event's own `ctrlKey`/`altKey`/`shiftKey`/`metaKey` flags, which are always accurate for whichever key just fired that event (including that key's own release), so no separate modifier-held tracking is needed. Tap-binds (Toggle Mute, Toggle Deafen) stay simple: `comboFromEvent(e) !== bound` on keydown, same as the old single-key check.
- **In-app markdown/code/PDF file viewer, full mechanism (2026-07-15)**: extensions in `ChatUI._viewableFileKind()` are a deliberate subset of `PeerManager._allowedExtensions`' "Text & code" group, kept in sync by comment since a file has to pass that allowlist to arrive at all. `.md` opens through `openCodeViewer`'s `'markdown'` branch (a real rendered document, not raw source); the rest of the text/code set opens in a new `#chat-code-viewer` singleton overlay (`_ensureCodeViewer()`, same reused-popover/focus-trap pattern as `_ensureLightbox()`/`openImageLightbox()` — shares its `.chat-lightbox`/`.chat-lightbox-action` action-bar styling). `.pdf`'s "View" button does `window.open(blobUrl, '_blank', 'noopener')` — no new UI, no CSP changes needed (an `<iframe>`/`<embed>` preview would have needed loosening `object-src 'none'`/adding `frame-src blob:`). Required extending `addFileMessage`'s signature (`ChatUI.js` *and* its `UIController.js` proxy) to thread the assembled `Blob` one layer further than before — it previously stopped at `UIController` (Files-tab/recap-zip use only) since `ChatUI` had never needed the raw bytes, only the `blobUrl` string.
- **Device-in-use card icons, full mechanism (2026-09-01, owner-reported: "only show what devices are being enabled by the user... don't want to see a mic icon on someone if it's not enabled for use")**: `UIController._updateParticipantStreamIcons(peerId)` toggles `.participant-screen-icon`/`.participant-cam-icon` (`display:none` unless `this.streams[peerId]`/`this.streams[\`${peerId}-cam\`]` holds a live stream), called from `addStream()`/`removeStream()` via a new `_realPeerIdForStreamKey()` helper that resolves a stream key (bare peerId / `${peerId}-cam` / `'me'`/`'me-cam'`) to the real peerId the card is keyed on. `_buildGridCell()` mirrors the same hide logic via a new `this._micHardwareOff` per-peer map (parallel to the existing `this._micEnabled` map) so a tile rebuild renders consistently without waiting on a fresh mic-status broadcast. For a remote peer this hidden/shown state rides on `mic-status`'s new `active` field.
- **Mobile front/back camera switch, the Samsung hardware reproduction (2026-08-10)**: Samsung phones (confirmed on a Galaxy S22) expose 3+ rear `videoinput` devices (wide/ultra-wide/telephoto) plus the front camera, and Chrome's `facingMode: {ideal: 'environment'}` can resolve to a non-primary lens that throws `NotReadableError` on open — even though a specific rear `deviceId` (e.g. picked via Settings' camera dropdown, `deviceId: {exact}`) opens fine. `PeerManager._acquireCamStream(facingMode)` tries a remembered per-direction deviceId first, falls back to `facingMode` ideal, and if that throws, probes the remaining `enumerateDevices()` videoinput entries (skipping whichever device is already confirmed for the *opposite* direction) until one actually streams.
- **Release mic when off, full mechanism (2026-09-01, owner-reported: Bluetooth headsets get downgraded to mono HFP the instant any app captures the mic, and normal mute doesn't release the hardware — the only workaround was leaving and rejoining the room)**: `PeerManager.toggleMic()`'s off-path, when the setting is on and the mic is currently enabled, calls `_releaseMicHardware()` instead of the ordinary `track.enabled = false` flip — stops every track on the raw stream, the noise-suppression pipeline's stream (if different), and the presence-detection clone (`_refreshPresenceMicStream(null)`). This is what actually lets the OS renegotiate the Bluetooth profile back to stereo A2DP — plain `track.enabled = false` keeps the hardware capture open all session, which is why mute alone never fixed it.
- **Voice-only mode, full mechanism (2026-07-10)**: opt-in (`localStorage['audioOnlyMode']`, Settings → Screen & Video, both `index.html` and `lobby.html` copies). The 4 call sites that used to unconditionally `_watchTile()` a stream on its own were the sole grid tile when `count <= 1`, `addStream()`/`updateLayout()`'s "nothing focused yet" auto-pick, and `removeStream()`'s auto-refocus after the watched peer leaves — all now go through `_autoWatchOrSkip()` instead.
- **Settings' sticky header, the bug narrative (2026-08-21)**: `.settings-content` is the scroll container (`overflow-y:auto`); the per-section `<h1>` used to live inside each `.settings-section` and the close button was `position:absolute` inside that same scrolling box, so both scrolled out of view once a section's fields ran long. Each `.settings-section`'s own `<h1>` was removed; `.settings-section-subcopy` and everything else still lives inside the section and scrolls normally — only the title and close button are pinned. `SettingsPanel._setCurrentTitle(section)` is called from both `open(section)` and `_wireNav()`'s click handler, the two places a section can become active.
- **No rich link-preview embeds, the removed-feature narrative (2026-08-24)**: a chat link used to get a small bordered "card" with a hostname badge underneath it (`.link-preview`/`.link-host` — removed), which an owner review called out as not actually useful without a real Discord-style embed (title/description/thumbnail scraped from the page). Non-image links (`ChatUI._processLinkPreviews()`) now render as plain inline text — only `target`/`rel` are still forced. `.chat-markdown a { overflow-wrap: anywhere }` (tailwind.css "Link previews" section) is what keeps a long unbroken pasted URL from forcing the chat panel to scroll horizontally.
- **@mentions, full mechanism (2026-07-09)**: `ChatUI._processMentions()` (called from `_finalizeMarkdownBody`, the shared post-render choke point for both plain chat and grouped file captions) matches `@nickname` against every current participant's nickname — supplied via a `getAllNicknames` constructor dependency, since `ChatUI` has no direct access to the participant list; `UIController._allNicknames()` provides it by querying `#participants .participant-name`. A message mentioning the local user gets a **persistent** `.chat-message-mentioned` bubble highlight (stays visible even after being read, unlike the transient `.chat-message-highlight` scroll-to flash) plus, gated on the same "not yet seen" condition as the general unread dot, a separate `#mention-indicator` "@" badge — cleared alongside `#new-message-indicator` wherever the chat panel is reopened (`App.js`'s `togglechat` handler).
- **Chat-message avatars, full history (2026-07-13, ends the 2026-07-12 scope cut)**: `ChatUI.addChatMessage()`/`ensureFileGroup()`/`addPollMessage()` all now take an optional trailing `senderPeerId` param (every call site in `App.js`/`PeerManager.js` threads it through — the local user's own peerId for a self-echo, the remote connection's peerId for an incoming message), feeding a `getAvatar` constructor dep (`ChatUI._getAvatar`, wired from `UIController.peerAvatars.get(id)`).
- **Members-panel per-peer actions, full CSS detail (2026-07-10)**: `:has()` rules keep the `.participant-actions` cluster (and the popover anchored inside it) alive while a volume/mod popover is open, and swap the passive icons out while the cluster is shown so the name doesn't get crushed — the kebab lives *inside* the cluster now, no longer a direct card child. `updateModeratorStatus()`'s `card.querySelector('.participant-mod-menu')` + inline `style.display` toggling is unchanged.

### Additional entries moved 2026-09-07 (seventh housekeeping pass)

Same treatment as the passes above — moved verbatim from CLAUDE.md's "Key conventions," each reduced there to a trimmed rule + pointer. This pass targeted the accounts feature's Phase 1-5 + real-usage-audit-fixes run, by far the largest single block that had accumulated in Key Conventions (design rationale, incident narratives, and Playwright-verification write-ups for each phase) — the load-bearing mechanism facts, security/authorization statements, and deploy-config requirements (Dockerfile/compose, session-token hashing, block/enumeration non-leak behavior, friends-only DM re-validation, honor-system-vs-server-authorized reasoning) all stayed in CLAUDE.md, trimmed. Also moved: the "Room events render inline in chat" and "Self-view PiPs can't be dragged under the room rail" entries (rule already fully captured in one sentence each), and the "Trust-tier indicator" entry — this supersedes the shorter, now-outdated 2026-09-06 version filed under the 2026-07-15 pass above (that version predates accounts Phase 4 tying `serverSideHistory` to `accountsEnabled`).

- **Room events render inline in chat, not as toasts (2026-07-07)**: `ChatUI.addSystemMessage(text, type)` renders a centered pill in `#chat-log` (`.chat-system-message`/`.chat-system-pill`, `type` one of `join`/`leave`/`stream`/`info` just picks the icon+accent color) — used for anything another peer's presence or the room state would explain: join/leave, moderator force-stop, quality-tier changes, bandwidth-pause evictions, blocked incoming/outgoing files. This replaced the old `showToast()` call for all of these, since a toast auto-dismisses after 4s and is invisible if it fired while the chat panel was closed, while the chat log is scrollback. `UIController.showToast()` still exists but is now single-style (`.toast`, no `type` param) and reserved for **purely local, non-room-event** feedback that isn't conversation-relevant and must stay visible even with chat closed: clipboard-copy confirmations (`InvitePopover.js`), camera/PiP permission errors. If you're about to add a new `showToast()` call, ask whether another peer's actions caused it or would care about it — if so it almost certainly belongs in `addSystemMessage()` instead. Chat avatars (`ChatUI._avatarInitials`, injected from `UIController._avatarInitials` via the `ChatUI` constructor) now show up to 2 letters (one per word) to match participant-card avatars, not just the first character.
- **Self-view PiPs can't be dragged under the room rail (2026-09-04)**: `_makeDraggable()`'s `clamp`/`pinToCorner`, plus `_reclampDraggedPip()`, all bound the PiP's left edge to `_roomRailReservedLeft()` (`#room-rail`'s live `getBoundingClientRect().right`, 0 when the mobile rail is slid off-screen) instead of `0` — previously the drag clamp only bounded the PiP to the window edges, so it could be dragged left far enough to overlap the fixed `#room-rail` strip (owner-reported). Also re-clamped once on load (`_makeDraggable`'s saved-position restore), so a position saved before this fix existed gets pulled back on the next page load too, not just on the next `resize`.
- **Trust-tier indicator (2026-09-06)**: `server.js` builds a deployment-level `trust` object (`serverSideHistory` was hardcoded `false` at launch — that feature didn't exist yet — and is now tied to `accountsEnabled` as of accounts Phase 4 (2026-09-07, direct messages persist plaintext content server-side once accounts are on; no independent "messages enabled" flag exists, so it rides the same gate rather than getting its own); `accounts` reflects real `ACCOUNTS_ENABLED` state as of the same-day Accounts entry below, was hardcoded `false` before that landed; `debugLogging`/`mediaRelayConfigured` are real, sourced from `utils/Debug.js`'s exported `DEBUG_ENABLED` and `!!turnConfig`) and passes it into `setupWebSocket()` as a 5th param, sent once per socket as a sibling field on `init` — deployment-level, not session-level, so it lives outside `SessionManager.getSessionMeta()` on purpose (that's per-room state). `App.js`'s raw `'init'` handler (not `PeerManager.handleSignal()`, since this isn't mesh/moderation state) calls `UIController.setTrustTier(trust)`, which composes a `dataset.tip` sentence on `#topbar-trust-icon` — tooltip-only in v1, no popover, same pattern as `updateMeshSignal()` above. Sits in `.topbar-left` directly after the brand icon (owner's call — a standalone badge there, not grouped with the room-state pills). Exists independently of any decision about whether Peek ever supports accounts/hosted deployments — it's meant to make what's already true today visible to an end user rather than only documented in this file. **Note on the pre-2026-07-07 room-name/lock/code and peer-count/signal pills**: a first pass this same session restored them alongside the new badge, reading their disappearance (`commit 147091a`, null-guarded dead code ever since) as an accidental reindent-commit casualty — the owner corrected this: the removal was deliberate (the info duplicates what's shown elsewhere, meant to eventually get a more meaningful home), so they were re-removed. `UIController.updateMeshSignal()`/`setHasPassword()` remain intentionally-inert no-ops. Server-side `trust` authority guarded by `tests/trust-tier.mjs`.
- **Accounts, Phase 1 (2026-09-06)**: opt-in (`ACCOUNTS_ENABLED=1`), SQLite-backed register/login/logout — see the Design Principles section above for why this is scoped as a per-deployment opt-in, and `TODO.md`'s "Deployment models" entry for the fuller conversation (SQLite chosen over MySQL specifically because it needs no network-facing service for a self-hoster to secure — the actual leak-risk driver is password-hashing discipline and file permissions, not the DB engine; username-only, no email, to avoid extra PII and an SMTP requirement). `server.js`'s `accountsEnabled` branch is the single gate: unset, zero new code executes (no DB file, no routes, `trust.accounts` stays `false`); set, it opens/migrates the DB before `mountAuthRoutes(app, authManager)` is called — **route mounting deliberately happens after `app.use(express.json())`**, not inline with the rest of the accounts setup, since Express dispatches middleware/routes in registration order and these handlers need `req.body` already parsed (getting this ordering wrong was caught during this same implementation — see `AuthManager.js`/`authRoutes.js`'s doc comments). Session tokens: opaque random bytes, only a SHA-256 hash is ever stored (mirrors password-hashing's "a stolen DB shouldn't hand out live credentials" reasoning), looked up via an indexed column rather than `creatorToken`'s `timingSafeEqual` compare (no DB to look itself up in there, hence the different approach) — and delivered via an `HttpOnly`/`SameSite=Lax` cookie rather than `localStorage`/`sessionStorage` like every other token in this app (`creatorToken`, `ownerTokens.js`), a deliberate divergence: an account session controls a person's whole identity, not a disposable room, so the XSS blast radius is categorically worse. **Two deploy-config fixes shipped alongside, both load-bearing, not optional**: `Dockerfile` now `npm rebuild`s `better-sqlite3` after the existing `--ignore-scripts npm ci` (that flag is deliberate supply-chain hardening for every *other* dependency — native packages' install scripts just don't run under it, so a naive add would ship a binary-less, crashing image); `compose.yml` now bind-mounts `./data:/app/data` (it had no volume at all — `deploy.yml`'s `docker compose down` + `up --build` on every push to `main` would otherwise wipe the accounts DB on every single deploy). Guarded by `tests/auth.mjs`. Phase 1 itself is API/schema only — see the Phase 1b entry below for the actual UI. **Fixed 2026-09-06 (owner-reported: "my local sqlite truncates/resets every time i do npm run dev")**: `openDb()`'s WAL mode means every commit lives ONLY in the `-wal` file until something checkpoints it back into the main `.db` file — SQLite's own automatic checkpoint threshold (~1000 WAL pages) is rarely reached by a small dev database's light write volume, so in practice nearly all data sat exposed in the WAL for the life of the process. `server.js` had zero shutdown handling at all, so every stop (Ctrl+C, a nodemon restart, a container SIGTERM) was a hard kill with no chance to checkpoint; a hard kill at exactly the wrong moment mid-write can leave the WAL failing validation on the next open, which SQLite handles safely by silently discarding it and reverting to the last checkpoint — for a dev DB that had never been gracefully closed, that's nothing. Two independent fixes, both load-bearing: (1) `server.js` now hoists the opened `Database` handle to `accountsDb` and calls `accountsDb.close()` (which forces a full WAL checkpoint) from new `SIGINT`/`SIGTERM` handlers before `process.exit(0)` — covers a real Ctrl+C in an interactive terminal correctly on Windows (console signal delivery works there even though `child_process.kill()` doesn't reliably deliver POSIX signals on Windows); (2) `package.json`'s new `nodemonConfig: {ignore: ["data/*"]}` stops nodemon from restarting the server on every single DB write in the first place (WAL/SHM files change on every commit) — this was almost certainly the dominant driver of the reported data loss, since it multiplied the number of kill-during-write windows by the write rate. Verified directly: insert a row, call `.close()`, confirm the WAL is fully checkpointed away and a completely fresh `Database` connection (simulating the next `npm run dev` start) sees the row.
- **Accounts, Phase 1b (2026-09-06)**: the login/register UI, `AccountPanel.js` (see its project-structure entry above). Also adds `GET /api/trust` (`server.js`) — the lobby page has no WebSocket connection at all (unlike the room page), so it had no way to learn whether this deployment has accounts enabled before deciding whether to show the account button; this route just exposes the same `trust` object already sent unauthenticated on every WS `init`, no new rate limit needed (cheap constant lookup, not a mutation). `#account-button` stays `hidden` in `lobby.html`'s markup until `AccountPanel._init()` confirms `trust.accounts === true` — same "never show a claim before it's confirmed" instinct as the room-page trust-tier badge. Session state on load comes from `GET /api/auth/me` (the httpOnly cookie is invisible to JS, so there's no other way to know if a returning visitor is already logged in). Register's confirm-password field is a client-side-only match check, no server round-trip for a mismatch — added specifically because Phase 1 has no password-reset mechanism, so a typo at registration has no recovery path otherwise. **Nickname/account sync (2026-09-06, same-day fix)**: `localStorage['nickname']` is unconditionally set to the account's username on every successful login/register and cleared on logout — a shared machine with multiple people each logging into their own account needs the room-display nickname to actually track whoever's currently logged in; the first version only pre-filled an *empty* nickname and never touched it on logout, which meant the previous person's name silently stuck around for the next person on that browser (whether they logged into a different account, or used the app without one at all — see `tests` note: verified manually via a two-account browser sequence, not (yet) a permanent automated test).
- **Accounts, Phase 1c (2026-09-06)**: syncs a deliberately small, fixed 6-key subset of preferences to a logged-in account — `theme`, `accentHue`, `bgTint`, `camDeviceId`, `micDeviceId`, `speakerDeviceId` — NOT the full ~30-key settings surface (owner asked for "preferred color scheme, camera, mic" specifically; see `TODO.md`'s "Deployment models" entry). Storage: a single nullable JSON-blob `TEXT` column (`users.settings`, migration `0002_add_user_settings.sql`) rather than a normalized table — six small fixed string keys always read/written as one unit doesn't justify one. `AccountSettingsSync.js` is the whole client-side mechanism: `syncPreference()` (called from inside `ThemeManager.setTheme()`/`AccentManager.setAccent()`/`BackgroundManager.setBackgroundTint()`/`PeerManager.switchCamera()`/`switchMicrophone()`/`UIController.setAudioOutputDevice()` — each of which already centralized persist+apply in one place, so **no `SettingsPanel.js` changes were needed at all**) debounces ~750ms and flushes on `pagehide` (`keepalive:true`) so a change immediately followed by closing the tab isn't lost; `pullAndApplySettings()` (called from `AccountPanel.js` on login and on an already-logged-in page reload) applies the 3 color-scheme keys live (safe from the lobby) but only writes the 3 device keys to `localStorage` — no `PeerManager`/`UIController` instance exists on the lobby page to hot-apply them to, so they take effect the next time that device is actually opened, identical to how a local device switch already behaves while the device is off. First login for an account with nothing saved yet bootstraps the account from whatever the browser already has, instead of leaving it empty. **Cross-machine device-ID risk, deliberately accepted**: WebRTC device IDs are browser+machine-specific and don't portably transfer to a different physical computer — for the shared-PC scenario that motivated this whole accounts feature (same machine, same hardware) syncing works perfectly; for a genuinely different machine, `PeerManager.js`'s `withDeviceFallback()` helper (wraps the 5 `getUserMedia({deviceId:{exact}})` call sites: `toggleCam()`, `switchCamera()`, `toggleMic()`, `_reacquireMic()`, `switchMicrophone()`) catches `OverconstrainedError` and retries without the device constraint, falling back to the system default instead of failing to turn the camera/mic on at all. **Deliberately does not self-heal** — it never clears the stale stored device ID on fallback, because a temporarily unplugged/busy/permission-revoked device on the *same* machine would hit the identical error, and clearing would risk the next unrelated setting change silently pushing the cleared value up and overwriting a working synced preference for the whole shared desktop; worst case without self-heal is just a repeated cheap fallback until the device reappears or the user explicitly repicks one. `applyCamQualitySettings()`'s own `deviceId:{exact}` usage is excluded from this fallback on purpose — it re-opens the *currently active* device's own just-read ID, not a synced cross-machine one, so it can't hit this failure mode. Guarded by `tests/account-settings.mjs` (API-level) plus manual browser verification (the live push/pull/bootstrap behavior — no automated test drives real camera/mic hardware in this repo's Playwright setup, same limitation noted for active-speaker/mic-presence testing elsewhere in this file).
- **Accounts, Phase 1d (2026-09-06)**: splits `camDeviceId`/`micDeviceId`/`speakerDeviceId` out of Phase 1c's account-wide blob into their own per-`(user, device)` table (`device_settings`, migration `0003_add_device_settings.sql`) — owner-reported: syncing device selection account-wide meant a phone and a desktop under the same account constantly overwrote each other's camera pick (the phone would inherit the desktop's meaningless hardware ID, and manually fixing that on the phone would push back up and clobber the desktop's own preference). `theme`/`accentHue`/`bgTint` stay account-wide in the original `users.settings` blob — genuinely meaningful on every device, unaffected by this split. `AccountSettingsSync.js`'s `getOrCreateDeviceId()` is the new piece: a random `localStorage['peekDeviceId']`, generated once per browser install and **deliberately never cleared on logout**, so it keeps meaning "this specific phone/desktop" regardless of which account (or none) is currently using that browser — same opaque-random-token-in-`localStorage` shape as `creatorToken`/session tokens elsewhere, just not a credential, purely a grouping key. `syncPreference()` now fires one `PUT` to `/api/auth/settings` and one to `/api/auth/device-settings` together on every synced change (simpler than tracking which bucket a given key belongs to); `pullAndApplySettings()` runs **two independent bootstrap checks** — an account can have global settings already from a previous device while this device has never saved its own row, or vice versa. `AuthManager.saveDeviceSettings()` upserts (`INSERT ... ON CONFLICT DO UPDATE`) since a device's row may not exist yet. Guarded by `tests/account-settings.mjs`'s device-settings scenarios (server-side) plus manual two-browser-context verification (the live cross-device behavior — same "no automated test drives real hardware" limitation as Phase 1c's device-selection sync itself).
- **Accounts, Phase 2 (2026-09-06)**: friends — a bare add/accept/remove relationship graph (`FriendsManager.js`/`friendsRoutes.js`/migration `0004_add_friendships.sql`), deliberately no presence and no DMs (both later phases, see `TODO.md`'s "Deployment models" entry — Phase 3 notes presence needs a genuinely new always-on connection this app doesn't have anywhere yet, so the friends list has no visible online/offline state today; it's purely relationship bookkeeping until then). `status` on a `friendships` row is `'pending'|'accepted'` only — declining, cancelling, or unfriending all delete the row outright rather than recording a third state, collapsing to `FriendsManager.removeFriendship()` as one verb for all three (matches the TODO's own "add/accept/remove" phrasing exactly, rather than inventing a 4th/5th method). **Mutual-pending auto-accept**: if B already has a pending request to A, A's own request to B accepts that existing row instead of leaving two simultaneously-pending rows for the same pair — a small UX call made during this implementation, not spelled out in the original TODO scoping. `sendRequest()` returns a discriminated `{ok:false, reason}` (`self`/`not_found`/`already_friends`/`already_pending`) rather than `AuthManager.js`'s plain falsy-on-failure convention — these are all outcomes a user needs distinct feedback on, and not a fresh enumeration surface (`/api/auth/register`'s 409 already confirms whether a username exists today). `authRoutes.js` exports `readCookie`/`COOKIE_NAME` for `friendsRoutes.js` to reuse rather than re-deriving the same cookie-parsing logic; `friendsRoutes.js` otherwise repeats the same inline session check every `/api/auth/*` route already uses, not a shared middleware, for consistency with the existing file's style. Client side, `FriendsPanel.js` (lobby-only, modeled on `AccountPanel.js`'s popover shape) shows its button only once accounts are enabled AND the viewer is logged in — the login half comes from a new `peek:account` `CustomEvent` `AccountPanel._renderState()` dispatches, the only cross-module signal for it on this page. Guarded by `tests/friends.mjs` (API-level, including the mutual-auto-accept and cross-user-authorization cases) plus a manual two-browser-context Playwright pass driving the real popover UI end-to-end (send/accept/remove). **Blocking added 2026-09-07** (found during a post-Phase-2 security review — see `TODO.md`'s former "No account-level block" Security entry, now shipped and moved to `CHANGELOG.md`): a separate `blocks` table (migration `0005_add_blocks.sql`), directional storage but `FriendsManager.isBlockedEitherWay()` checks both directions since a block needs to be mutual for request purposes despite only the blocker's row existing. `sendRequest()` checks this before its existing self/relationship checks and folds a hit into the same `not_found` reason (never a distinct `blocked` one) — neither the blocker nor the blocked party can confirm a block exists by probing, the identical non-enumeration instinct the reason-code design already had. `blockUser()` deletes any existing `friendships` row for the pair in the same call. `listBlocked()` only returns the caller's own outgoing blocks, so a block stays invisible to the person blocked. New routes `POST /api/friends/block`/`DELETE /api/friends/block/:id` (two path segments, so no collision with the existing single-segment `/api/friends/:id` DELETE despite sharing a method). `FriendsPanel.js` gained a "Block" action on friends/incoming rows and a "Blocked" list with Unblock. Guarded by 15 new `tests/friends.mjs` assertions.
- **Accounts, Phase 3 (2026-09-07): presence, poll-based.** The Phase 2 entry above's "presence needs a genuinely new always-on connection" framing was the *original* Phase 1 scoping — a same-day security/load review revised that (numbers and full reasoning in `CHANGELOG.md`'s Accounts section) and this ships the revised plan instead of a socket. `GET /api/friends/presence` (`friendsRoutes.js`, rate-limited 30/min) is deliberately a separate route from `GET /api/friends`, not a field added to it — the latter is fetched only when the `FriendsPanel.js` popover opens, the former is polled every 30s by `presencePoll.js` regardless of whether the popover is even open. **The poll request IS the heartbeat** — its handler calls `AuthManager.touchLastSeen(session.userId)` as a side effect before ever reading anyone else's presence, so there is no separate heartbeat endpoint; a user who never calls this route (accounts enabled but the lobby never open, or the poller stopped) simply has a stale/NULL `last_seen_at` and reads as offline to everyone. "Online" is derived, not stored: `FriendsManager.listFriendsPresence(userId, onlineThresholdMs)` computes it at read time against `last_seen_at`, `onlineThresholdMs` = 90s (3x the 30s poll interval, tolerates one missed beat without flickering). New nullable `users.last_seen_at` column (migration `0006_add_last_seen.sql`) — a column, not a table, same "one small value" reasoning as the Phase 1c settings blob. Client: `presencePoll.js` (new file, `startPresencePolling(onUpdate) → stop`, identical schedule-after-tick/pause-on-visibilitychange shape to `roomStatusPoll.js`) is started/stopped by `lobby.js` on the existing `peek:account` event — **must actually stop on logout**, since a poller left running against a logged-out session would 401 forever rather than just going unused (a real "does this survive login/logout, not just login" case, same class of bug the Phase 1b nickname-sync fix was about). `FriendsPanel.js` caches the last-fetched friends array (`_lastFriends`) so a `presencePoll.js` tick's `setOnline()` call can cheaply re-render only the friends-list dots (`_renderFriendsList()`) without re-hitting the heavier `GET /api/friends` every 30 seconds — that fetch stays scoped to popover-open. **Deliberately minimal, not full presence**: lobby-only (no room-page equivalent — `AccountPanel.js`/`FriendsPanel.js` don't exist there today), a plain two-state online/offline dot (`.friends-presence-dot`), no "last seen 5m ago" text, no away/idle tier. **The persistent-socket alternative from the original Phase 1 scoping was deliberately not built** — see `TODO.md`'s Phase 3 entry for the three non-negotiables (`Origin` validation, server-derived recipient lists, invalidate-on-unfriend) that would apply if it ever is, e.g. for Phase 4's DM push delivery. Guarded by `tests/presence.mjs` (API-level: never-polled friend offline, polling-is-the-heartbeat, pending requests excluded, no-cookie/accounts-disabled) plus a real two-browser-context Playwright pass through the actual lobby UI, including waiting out the genuine 30s poll interval to see the dot flip live rather than mocking the timer.
- **Accounts, Phase 4 (2026-09-07): direct messages, poll-based, plain-text.** Closes out the "full social layer" scope decided at Phase 1 (accounts → friends → presence → DMs, see `TODO.md`'s Deployment models entry). Follows Phase 3's precedent rather than the *original* Phase 1 scoping's plan (a `direct_messages` table needing "Phase 3's connection to feel real," i.e. push delivery) — since Phase 3 ended up poll-based, DMs poll too (`messagesPoll.js`, `GET /api/messages` every 30s, identical shape to `presencePoll.js`) rather than becoming the feature that finally forces the always-on socket into existence. New `direct_messages` table (migration `0007_add_direct_messages.sql`) + `DirectMessagesManager.js`/`messagesRoutes.js`: `GET /api/messages` (inbox — conversation partners sorted by most-recently-active, each with a last-message preview and unread count), `GET /api/messages/:username` (a conversation thread — **also where the recipient's unread messages in it get marked read**, a side effect of `DirectMessagesManager.getConversation()`, no separate "mark read" call exists anywhere), `POST /api/messages/:username` (send). **Friends-only, re-validated on every send** — `sendMessage()` calls the identical accepted-friendship check `FriendsManager._findRelationship()`-adjacent logic already uses (`_isFriend()`, a private helper on `DirectMessagesManager` itself, not a call back into `FriendsManager` — kept local since it's a one-line query, not worth a cross-manager dependency for). Since `FriendsManager.blockUser()` already deletes the `friendships` row, a block transparently blocks new DMs too with **no separate block-check anywhere in the messages code** — this was a deliberate design goal, not a lucky accident: message history from before an unfriend/block is preserved (`listConversations()` is keyed off actual message history, not current friendship status), only new sends are rejected. **`trust.serverSideHistory` flipped from its hardcoded `false`** to track `accountsEnabled` (`server.js`) — this is the feature that flag was built in anticipation of; `tests/trust-tier.mjs` gained a 4th scenario confirming it. Client: `MessagesPanel.js` (lobby-only popover, inbox + thread views) reachable via its own `#messages-button` (unread badge fed by `messagesPoll.js`) or `FriendsPanel.js`'s new "Message" row action (a `peek:open-dm` `CustomEvent`, same cross-popover-signal shape as `peek:account` — the two popovers have no direct reference to each other, both are independently constructed by `lobby.js`). **Two deliberate v1 scope cuts, not oversights**: (1) plain-text rendering — `_renderThread()` uses `textContent` for every message body, not the `marked`→DOMPurify pipeline `ChatUI.js` uses for room chat; still fully safe against XSS (no HTML interpretation at all), the cut is markdown/link/mention/emoji formatting, not security, and upgrading later is additive, not a rewrite. (2) no read-receipt UI beyond the unread count, no pagination past the most recent 200 messages (`DirectMessagesManager`'s `CONVERSATION_LIMIT`), no push — all likewise additive later. Guarded by 24 `tests/messages.mjs` assertions (friends-only enforcement incl. self-DM/nonexistent-recipient, body validation, ordering + per-side `fromMe`, unread-count lifecycle, inbox sort order, history-survives-unfriend, no-cookie/accounts-disabled) plus a real two-browser-context Playwright pass through the actual lobby UI end-to-end (register both accounts, befriend, deep-link from Friends into a new conversation, send, confirm the recipient's inbox preview and unread badge, open and reply, confirm the badge clears on read and the sender sees the reply — zero page errors either side).
- **Accounts, Phase 5 (2026-09-07): in-room friending bridge, opt-in.** Closes part of the "add friend directly from a room, not just the lobby" TODO gap. The room's WebRTC mesh otherwise has no concept of accounts at all — bridging the two identities (P2P peerId vs. server-side account) is a new Settings → Privacy & P2P toggle, **`#settings-reveal-account`/`localStorage['revealAccountInRoom']`, off by default**: "Reveal my account to peers in this room." Turning it on calls `PeerManager.refreshAccountReveal()`, which fetches `/api/auth/me` and, if logged in, calls `setAccountUsername()` — broadcasts a new `account-username-update` WS relay message (added to `WebSocketServer.js`'s existing broadcast fall-through list alongside `avatar-update`/`nickname-update`/`status-update`) to every peer in the room, re-sent via `broadcastAccountUsername()` on every `peer-joined` (same pattern as avatar/status/nickname) so late joiners get it too, and via `refreshAccountReveal()` itself on every `'init'` (fresh join/reconnect). Receive side: `PeerManager`'s `'account-username-update'` case calls `UIController.updateParticipantAccountUsername(peerId, username)`, which reveals a small "Add Friend" button (`_buildAddFriendControl()`) on that peer's card — clicking it POSTs to the existing `/api/friends/request` using the *clicking* user's own session, deliberately independent of whether the clicker has also opted in to revealing themselves (sending a request only needs them logged in, checked server-side on click; a 401 there just means "log in from the lobby first," surfaced via `showToast()`). `peerAccountUsernames` is session-scoped like `peerAvatars`/`peerVolumes` — cleared in `clearAllParticipants()`, pruned in `removeParticipant()`, since peerIds don't survive a reconnect. The revealed username is honor-system like nickname/status/avatar — never trusted for anything beyond deciding whether to show the button; the actual friend request is authorized server-side against the sender's own session, not this payload. **Deliberately opt-in, not automatic** (unlike, say, nickname) — revealing an account username to a room full of strangers is a real privacy step up from anything else broadcast in this mesh, closer in kind to the Phase 3 presence privacy discussion than to ordinary display-name broadcasting.
- **Accounts real-usage audit fixes (2026-09-07)**: a deliberate re-read of the Phase 1-4 code against actual two-person usage (owner's own words: "I'm sure there is a lot overlooked during the planning") found four real gaps, all fixed same day — full narrative in `CHANGELOG.md`'s Accounts section, brief pointers here since each also has its own file-level note above: (1) an open DM thread never live-updated (`MessagesPanel._pollActiveThread()`), (2) friend-list row actions failed completely silently on a 4xx (`FriendsPanel._runAction()` + `#friends-action-error`), (3) a second open tab never learned it had been logged out elsewhere (`presencePoll.js`/`messagesPoll.js`'s new `onSessionExpired` param → `AccountPanel.forceLoggedOut()`), (4) rate limiting on `/api/friends`/`/api/messages` routes was inconsistent — some routes had none, and every limited one was keyed by IP rather than session, so two friends behind the same NAT could throttle each other (`rateLimit()`'s new `keyFn` param, `friendsRoutes.js`/`messagesRoutes.js`'s `byAccount`).

### Additional entries moved 2026-09-10 (eighth housekeeping pass)

Same treatment as the passes above — moved verbatim from CLAUDE.md's "Key conventions," each reduced there to a trimmed rule + pointer. This pass targeted the DM/chat-parity push's own incident narratives (composer parity, thread componentization, reserved self-color + avatar sync, consecutive-message grouping, the icon-only tab bar's wrong-first-cut story) plus the two-round accounts UI-consolidation redesign story — all from the 2026-09-06 through 2026-09-10 accounts/DM work, the single densest run of dated entries in the file (158 of ~340 total date-stamps).

- **DM composer parity with room chat (2026-09-09, owner-reported)**: "can we not reuse the same input area as chat so we get all the other features too like code blocks and emoticons." Two shared modules extracted so both composers stay in sync rather than duplicating logic: **`composerUtils.js`** (`insertAtCaret()`, `wireComposerPlusMenu()`, `isInsideOpenCodeFence()`, `autoGrowTextarea()` — all four moved out of `App.js`, which now just calls them) and **`markdownCodeBlocks.js`** (`finalizeCodeBlocks()`, extracted out of `ChatUI._finalizeMarkdownBody()`). `MessagesPanel.js`'s composer gained the identical "+" dropup (`#messages-composer-plus-btn`/`#messages-composer-plus-menu`, markup duplicated between `index.html`'s room-tab instance and `SocialPanel.js`'s self-built lobby/room-Friends-modal instance, same as `#messages-input` itself already was) with **Code block** (`openCodeBlockPicker()`) and **Add emoji** (`openEmojiPicker()`) options — both singleton pickers, already shared with the room composer, needed zero changes to be called from a second file.
  - **Rendering is feature-detected, not unconditional**: `_renderThread()` checks `typeof marked !== 'undefined' && typeof DOMPurify !== 'undefined'` and renders via the same `marked.parse()` → `DOMPurify.sanitize()` → innerHTML pipeline room chat uses (plus `finalizeCodeBlocks()` for hljs highlighting/lang-label/copy-button) when true, falling back to the original plain-`textContent` render otherwise. **This isn't a security fallback — reusing ChatUI's exact pipeline is the same already-audited pattern, not a new attack surface.** It's a *page* fallback: `index.html` (the room) loads `marked`/`DOMPurify`/`highlight.js` (ChatUI.js already needs them there), but `lobby.html` never has and still doesn't — growing the lobby's initial payload for a feature only reachable after logging in and friending someone wasn't judged worth it. `_wireComposerExtras()` hides the "Code block" +-menu option entirely (`codeBlockBtn?.remove()`) on a page without `hljs`, for the identical reason: offering it would just insert literal backticks nothing highlights.
  - **Real bug found while testing multi-line code in the DM composer**: `_wireSend()`'s keydown handler treated *every* Enter as send, with no Shift+Enter exception and no open-fence exception — unlike the room composer, which already had both (`isInsideOpenCodeFence()`). Typing a multi-line code block was therefore impossible: the first Enter inside the fence sent the message half-typed. Fixed by giving `MessagesPanel.js`'s keydown handler the identical two checks the room composer's already had, via the now-shared `isInsideOpenCodeFence()`. Caught by a real two-account Playwright pass that used `page.keyboard.insertText()` (which doesn't dispatch discrete Enter keydown events) specifically *because* `page.keyboard.type()` with an embedded `\n` reproduced the exact same bug a real user's Enter key would have hit.
  - **Second real bug found via the same test, in shared CSS this exposed rather than caused**: `.chat-markdown pre`'s `.code-lang-label`/`.copy-btn` are absolutely-positioned over the code block's top-right corner, relying on there usually being enough right-margin slack on the first code line for them not to visually collide with it — true often enough in room chat's wider panels, but a short one-line function signature in `.messages-bubble`'s much narrower width (`max-width: 80%` of an already-narrow drawer) collided immediately. Fixed by giving `.chat-markdown pre` real top padding (`1.75rem`, was `0.6em` all around) so the label/button always render in reserved space above the code rather than overlaying its first line — a correctness fix for every context this styling is used in, not a DM-specific patch (verified room chat's own code blocks still render correctly, now with slightly more breathing room above the code too).
  - **Composer auto-grow**: `autoGrowTextarea()` (was App.js-local `autoGrowMessageInput()`) is now called from `MessagesPanel.js` too — on every `input` event and after each picker-driven insert (`insertAtCaret()` mutates `.value` directly, firing no real `input` event) — since a DM composer holding a real multi-line code fence needs the same grow-to-fit `.chat-composer-input` already has (`max-height: 6em` CSS cap, `overflow:auto` beyond that), not just a fixed single row.
- **DM thread mimics room chat, not a separate bubble style (2026-09-10, owner-reported)**: "this just needs to mimic the chat panel rather than have a different style and input area... these will more than likely need to be done for messages also" — the 2026-09-09 DM composer parity work above cloned the composer's *behavior* by hand into `MessagesPanel.js`, but the actual wiring was still two independent copies (App.js's + MessagesPanel.js's own), and the DM thread itself still rendered as a two-sided `.messages-bubble` list instead of looking like room chat at all. Two real componentizations, not just shared utility functions this time — the goal being that a future chat improvement only needs one call site changed to reach both surfaces:
  - **Composer wiring is now one shared call, not two hand-copies**: `composerUtils.js`'s new `wireComposerExtras({plusBtn, plusMenu, codeBlockBtn, emojiBtn, input})` owns the "+" menu's open/close plus the code-block/emoji click handlers (`wireCodeBlockButton()`/`wireEmojiButton()`, each just the insert-at-caret + auto-grow + focus logic, anchored back at `plusBtn` for the same reason the old per-file versions were), and `wireEnterToSend(input, onSend)` owns the Shift+Enter/open-fence-aware Enter-to-send rule. App.js's room composer and MessagesPanel.js's DM composer both just call these two functions now — App.js's file-attach/poll buttons stay wired separately (room-only, no DM equivalent), since `wireComposerPlusMenu()` already closes the menu generically on a click on *any* button inside it regardless of who wired that button.
  - **Message rendering is now a shared row builder, not a bubble**: new `chatMessageRow.js` extracts `colorForName()`/`avatarHtml()`/`messageHeaderHtml()` out of `ChatUI.js` (`_colorForName()`/`_avatarHtml()` now delegate to it, `addChatMessage()` builds its header through `messageHeaderHtml()` — byte-identical output, pure extraction, verified via `tests/chat-edit-delete.mjs` still passing unmodified). `MessagesPanel.js`'s `_renderThread()` now calls the same `messageHeaderHtml()` to build each DM as a real `.chat-message` row (avatar+sender+timestamp header, markdown body below) — the exact same DOM shape and CSS classes (`chat-message`/`chat-avatar`/`chat-sender`/`chat-timestamp`/`chat-body`) room chat's `#chat-log` uses, just appended into `.messages-thread` instead. The old `.messages-bubble`/`.messages-bubble-mine` CSS was removed outright (tailwind.css). Own messages show the real logged-in username (`localStorage['nickname']`, unconditionally synced to the account's username on login — see `AccountPanel.js`'s "Accounts, Phase 1b" entry) in the same green self-color (`#22c55e`) room chat uses for a self-echo — **never a "Me" sentinel**, consistent with this file's own standing "No 'Me' sentinel" rule just below; the other party's messages hash to a deterministic color from their username, same as any room-chat nickname. Real timestamps come from `DirectMessagesManager`'s existing `createdAt` field (already returned by the API, just never rendered before).
  - **`initial`/`color` stay caller-computed, not derived inside the shared function** — `ChatUI.js`'s real avatar-initials logic is injectable per-instance (`UIController._avatarInitials()` takes up to 2 chars from a multi-word room nickname, since those can contain spaces) while `chatMessageRow.js`'s own `avatarInitial()` is a simpler one-char version correct only for MessagesPanel.js's single-token account usernames — `messageHeaderHtml()` takes both precomputed rather than assuming one algorithm fits both surfaces.
  - **Deliberately still NOT shared**: reactions, replies, @mentions, pins, edit/delete ownership, history persistence — room-chat-only concepts a DM thread doesn't have (`DirectMessagesManager.js` cut them for v1, see "Accounts, Phase 4" entry). `chatMessageRow.js` only owns the header/avatar/color pieces both surfaces genuinely need identically; `ChatUI.js` layers everything else on top of that same header shape itself.
  - Verified via a real two-account Playwright pass (not kept in `tests/`, this is a rendering/wiring refactor of an existing feature rather than new server-enforced behavior `messages.mjs` would need to cover): a sent DM renders as a `.chat-message` row with avatar/sender/timestamp/body, the sender's own message shows their real username in the shared green self-color, the other party sees the same message with a hashed (non-green) color, Enter inside an open code fence still inserts a newline instead of sending, and a code-block message still gets real hljs highlighting via the shared `markdownCodeBlocks.js` pipeline — plus `npm run test:offline`/`test:ui`/`test:chat-edit` all still pass unmodified (the `addChatMessage()`/`ChatUI.js` refactor is output-identical).
  - **Same-day follow-up (owner-reported via screenshot): the room-tab thread was capped at a fixed small height with a lot of dead space below the composer, instead of filling the tab like `#chat-log` does.** Root cause: `#messages-tab-content`/`#messages-conversation-view` are toggled with a plain `classList.toggle('hidden', ...)` and never carry Tailwind's own `flex` utility class (`flex-col`/`flex-1`/`min-h-0` alone don't set `display`) — unhiding them left `display` at its default `block`, so `.messages-thread`'s own `flex: 1` (and `#messages-inbox-view`'s `flex-1` utility) silently did nothing, since neither had an actual flex container to grow inside. The lobby/Friends-modal host shape already had the equivalent `:not(.hidden){display:flex}` fix (see the `.social-tab-panel[data-social-panel="messages"]` rule elsewhere in this file) — the room-tab context just never got the same treatment when it was added. Fixed with the identical pattern, scoped by id: `#messages-tab-content:not(.hidden)`/`#messages-conversation-view:not(.hidden)` (tailwind.css). `.messages-thread`'s base rule switched from a fixed `max-height: 16rem` to `flex: 1; min-height: 0` (the now-redundant `.social-tab-panel[...] .messages-thread` override was removed — one rule now covers both host shapes). **Second half of the same report ("with the auto scroll and everything like chat window")**: a new `chatAutoScroll.js` extracts `isAtBottom()`/`scrollToBottom()`/`scrollIfAtBottom()`/`wireAutoScrollResize()` out of `ChatUI.js` (`_isAtBottom()`/`_scrollToBottom()`/`_wireAutoScrollResize()` now delegate to it, byte-identical behavior) — `MessagesPanel.js`'s `_renderThread()` used to unconditionally jump to the bottom on every render, which would have yanked a user reading upward DM history back down the instant a live `messagesPoll.js` tick delivered a new message (the exact bug room chat's own `_scrollIfAtBottom` already avoids). `_renderThread()` gained a `forceScroll` option: `true` for a fresh conversation open (`_loadConversation()`, always an instant jump — same as ChatUI's own bulk-history-load behavior), unset for a live poll-tick update (`_pollActiveThread()`, only scrolls if already at the bottom, smoothly). The DM thread's constructor also calls `wireAutoScrollResize()` so a chat-panel drag-resize re-pins it to bottom exactly like `#chat-log`. Verified via the same Playwright pass: `#messages-tab-content`/`#messages-conversation-view` are real flex containers once shown, the thread's rendered height is a large fraction of the tab (not the old ~256px cap), and `isAtBottom()` correctly flips false/true as a synthetic overflowing container is scrolled up/down.
- **Reserved self-color + account avatar sync (2026-09-10, owner-reported, with screenshots proving both)**: two related identification fixes to room chat and DMs together, prompted by real usage — "a safeguard to make sure that the client always has its own unique color that can not be picked by participants" plus "avatars are missing in the messages."
  - **Reserved self-color** — full detail in the standalone "Reserved self-color" entry elsewhere in this file; summary here: `chatMessageRow.js`'s `SELF_COLOR`/`colorFor()` guarantee no participant's hashed color can ever equal the local user's own (the 12-color hash palette dropped to 11, excluding `'#22c55e'` outright). The owner's screenshot showed exactly the failure mode this closes: from one account's perspective, its own messages *and* the other party's both rendered green, because the other party's username happened to hash into the same color reserved for self.
  - **Account avatar sync** — the account schema already reserved a `users.avatar` column (`0001_init.sql`) but nothing had ever written to it; a room-only avatar (`localStorage['avatarDataUrl']`, `SettingsPanel.js`'s picker) never carried into DMs, which are server-mediated between accounts with no P2P broadcast to fall back on — so DMs always showed initials only, even for someone who'd clearly set a custom avatar. Fixed with a small, separate (not `GLOBAL_KEYS`-bundled — see `AccountSettingsSync.js`'s entry above for why) sync pair: `AuthManager.saveAvatar()`/`getAvatar()` (same `data:image/(webp|jpeg|png);base64,...` regex + 40,000-char cap as `PeerManager.js`'s client-side `_isValidAvatarDataUrl()` — a real trust boundary now that it's server-persisted, not just P2P-broadcast to peers who already see everything from that session) behind `GET`/`PUT /api/auth/avatar`. `SettingsPanel._wireAvatar()`'s set/remove handlers call `AccountSettingsSync.syncAvatar()` (immediate, undebounced — an avatar pick is a deliberate one-off, not a rapid-fire slider). `pullAndApplySettings()` gained a third independent bootstrap check (mirroring Phase 1c/1d's own two): if the account has an avatar, pull it into `localStorage['avatarDataUrl']` (account wins, cross-device source of truth); if not but this device already has a local one, push it up — this is the exact gap the owner hit ("i have one... but its not part of my account... when i registered").
  - **Wire-up for display**: `DirectMessagesManager.getConversation()` now also returns `partnerAvatar` (the other party's `users.avatar`); `listConversations()` already returned it via `_publicProfile()`, just never rendered. `MessagesPanel._renderThread()` uses `localStorage.getItem('avatarDataUrl')` for own messages and `this._partnerAvatar` (refreshed by both `_loadConversation()` and the live `_pollActiveThread()` poll tick) for the other party's, falling back to initials when either is unset — same `avatarHtml()`/`avatarInitial()`/`colorForName()` fallback chain room chat already uses. `_renderInbox()`'s rows also gained a small avatar (the data was already in `GET /api/messages`'s response, just unused).
  - **Third cue, owner-suggested as a fallback ("at least my messages could have a different shade for the background")**: a new `.chat-message-self` class (tailwind.css, a subtle wash via `color-mix(in srgb, var(--green) 8%, transparent)`) applied to every own-message row in both surfaces (`ChatUI.js`'s `addChatMessage()`/`addPollMessage()`/`ensureFileGroup()`, `MessagesPanel.js`'s `_renderThread()`) — a third, redundant "this is mine" signal (color, avatar, *and* background) rather than relying on any single one. `.chat-message:hover`'s existing highlight still wins on hover by CSS specificity (`:hover` pseudo-class beats a plain class), unchanged.
  - Verified via two real two-account Playwright passes (server-avatar-bootstrap-on-first-login, and a full round-trip: Alice's real account avatar rendering as an actual `<img>` — not initials — in Bob's DM thread and inbox row, Alice's message never green in Bob's view) plus a visual screenshot confirming the green tint/avatar/color trio together — not yet a permanent file in `tests/`, all four existing accounts/messages test files (`auth.mjs`/`account-settings.mjs`/`friends.mjs`/`messages.mjs`) still pass unmodified since none of their assertions pin the exact response shape this added fields to.
- **Consecutive-message grouping (2026-09-10, owner-requested, Discord-style)**: "if i send 1 and then i send 2... it should be my name 1 and then a new line shows 2 since they grouped... but if my friend typed before i typed 2 then it would be my name 1, friend something, my name 2." A message from the same author as the one immediately before it (no other message of any kind in between) renders with no header of its own — `.chat-message-grouped` (tailwind.css) just tightens `padding-top`, the avatar/sender/timestamp `messageHeaderHtml()` call is skipped entirely for that row.
  - **ChatUI.js (room chat)**: `_lastGroupKey` (instance field, constructor-initialized to `null`) tracks the author key (`senderPeerId || sender`) of the last-appended plain chat message, since messages are appended to `#chat-log` one at a time across separate method calls rather than rebuilt from an array. `addChatMessage()` groups when `this._lastGroupKey === (senderPeerId || sender)` — **except a reply**, which always shows its own full header regardless (the reply-quote block above it already makes the message visually distinct from a plain continuation line) — and always updates `_lastGroupKey` afterward. Every other render that isn't a continuing plain chat message calls the new `breakMessageGrouping()` (sets `_lastGroupKey = null`) so it can't be silently grouped with unrelated content: `addPollMessage()`, `ensureFileGroup()` (only on an actual new group's creation, not later files joining an existing one via `_fileSlot()`), `addSystemMessage()`, and `UIController._loadChatHistory()` right after appending its "Today" divider (a divider inserted directly into `#chat-log` from outside `ChatUI`, so it can't update `_lastGroupKey` on its own).
  - **Delete must not orphan a grouped successor**: `applyChatDelete()` removes a message outright (no tombstone, per the standing rule above) — deleting a header-bearing group leader would otherwise leave the next message in that run as a headerless, sender-less orphan. `_promoteNextGroupedIfOrphaned(container)` (called before the actual removal, while `container.nextElementSibling` is still reachable) checks whether the message about to be deleted was itself a group leader (not `.chat-message-grouped`) and, if its next sibling is a grouped continuation, rebuilds and inserts a real header into it from `_messageMeta` (now also storing `senderPeerId`, added solely for this) and strips its `.chat-message-grouped` class. Separately, deleting the *newest* message in the log (no next sibling at all) calls `breakMessageGrouping()` instead — `_lastGroupKey` would otherwise still point at a now-deleted message, and the next *new* message from that same author would wrongly render as a continuation of something invisible; resetting is a simpler, always-safe fix than working out whether some earlier still-visible message could legitimately continue the run (worst case, one redundant header shows where a grouped one technically could have).
  - **MessagesPanel.js (DMs)**: much simpler — `_renderThread()` rebuilds the whole thread from the `messages` array on every call (no incremental-append state to track across calls), and a DM only ever has two possible senders, so a local `let lastFromMe = null` loop variable comparing consecutive `fromMe` values is a complete author key. No delete/orphan concern either, since DMs have no edit/delete (`DirectMessagesManager.js` cut them for v1) and the full rebuild recomputes grouping fresh every time regardless.
  - Verified via two real Playwright passes (room chat two-peer, DM two-account): a first message shows its header, an immediate second message from the same sender groups (no header, `chat-message-grouped` class present), a message from the other party in between forces the next same-sender message to show its own header again, and — the trickiest case — deleting a room-chat group leader promotes its grouped successor to a full header instead of leaving it orphaned. `npm run test:offline`/`test:ui`/`test:chat-edit`/`test:chat-history` all still pass unmodified.
- **Icon-only chat tab bar (2026-09-09, measured in JS, not a CSS breakpoint — see below for why)**: the Chat/Files/Messages tab bar (`#tab-chat`/`#tab-files`/`#tab-messages`, each tab's text now wrapped in a `<span class="panel-tab-label">`) collapses to icon-only once the row's real content no longer fits alongside the trailing search/pinned/close icons. `data-tip` is set on all three tab buttons unconditionally (not just once labels hide) so Tooltip.js's hover label is always available. **First cut used a `@container chat-panel (max-width: 20rem)` query and was wrong** (owner caught it live, screenshot showed a case it mishandled): a single fixed width can't correctly cover every tab/badge combination, since the row's actual content-width requirement varies a lot with how many tabs are visible (Messages is accounts-gated — 2 tabs comfortably fits text down to the panel's own floor width, 3 tabs needs real room) and whether the Files/Messages unread badges are showing (a "99+" badge is meaningfully wider than none). One fixed threshold is always wrong for at least one of those combinations — either hiding labels needlessly early for the common 2-tab case, or (the bug that shipped first) leaving a "danger zone" width range where the query kept labels shown even though 3-tab content genuinely overflowed there. **Fixed by measuring instead of guessing**: `UIController.updateTabBarCompact()` temporarily strips its own `.tabs-compact` class (a no-op mutation if not already present, so this can't self-trigger the ResizeObserver below), compares `.panel-tabs`' real `scrollWidth` against its `clientWidth`, and toggles `.tabs-compact` (CSS: `.panel-tabs.tabs-compact .panel-tab-label { display: none; }`) based on the actual answer — correct for whatever tabs/badges are actually on screen, no per-combination threshold to maintain. Called from: `_initTabBarCompact()` (once, wiring a `ResizeObserver` on `.panel-tabs` for panel-width/drag-resize changes, plus a `window resize` listener — `.panel-tabs`' own box doesn't change with the fluid clamp() root font-size, only its rem-sized children's *natural* size does, so width-only observation would miss that); `_addFileToTab()` (files badge appearing); `App.js`'s `setRoomMessagesLoggedIn()` (Messages tab itself appearing/disappearing); and a `peek:messages-badge-changed` CustomEvent `MessagesPanel.js`'s `_updateBadge()` dispatches (same cross-module-signal shape as `peek:account`/`peek:open-dm` — no direct reference from that file to `UIController.js`). `.chat-panel`'s `min-width` (17rem) is set to the narrowest width the icon-only layout still fits at (worst case: all three tabs plus both unread badges) — below that, both the CSS min-width and `App.js`'s `initChatResize()` drag handler refuse to shrink further. **`min-width: 17rem` is not a constant 272px** — this app's root font-size is a fluid `clamp()` (`FontScaleManager.js`/tailwind.css's `:root`), so its pixel value shifts with viewport width; `initChatResize()` reads it live via `getComputedStyle(chat).minWidth` rather than hardcoding a px guess, and any future JS that needs to reason about this floor should do the same rather than assuming 16px/rem.
- **Accounts, UI consolidation, round 1 (2026-09-08): Friends/DMs reachable from a room, not just the lobby.** Owner feedback after real usage: DMs only existed on the lobby page, so two friends already in a call had no way to message each other without leaving it. First pass consolidated `AccountPanel.js`/`FriendsPanel.js`/`MessagesPanel.js` (previously three separate lobby-only anchored popovers) into one self-building modal, `SocialPanel.js`, cloning `SettingsPanel.js`'s nav-sidebar/sticky-header shell and adding Account as a third tab alongside Friends/Messages. **Superseded same-day by round 2 below** — the owner's reaction to actually using it was that it looked like a re-skinned Settings panel and Account (holding nothing but a sign-out button once logged in) wasn't a real section. What's still true from round 1: constructed by **both** `lobby.js` and `App.js` (previously App.js had none of this), a modal overlay rather than a Discord-style dedicated DM view/navigation (the owner was explicit Peek's room should stay the main view), and each sub-panel's own business logic (fetches/rendering, the whole `/api/auth|friends|messages/*` contract) is completely unchanged across both rounds — zero backend/API changes either time.
- **Accounts, UI consolidation, round 2 (2026-09-08): Account moved out into each page's identity area; SocialPanel.js redesigned to not look like Settings.** Two changes, both owner-directed:
  - **Sign in/register/out no longer lives in a panel tab.** `AccountPanel.js` is now independently constructed by `lobby.js`/`App.js` (not by `SocialPanel.js` — the two share no reference, only the `peek:account`/`peek:force-logout` CustomEvents below) and injects itself into whichever identity affordance each page already has: **room** — two new static rows in `TopbarIdentity.js`'s existing `#topbar-identity-menu` dropdown (`#topbar-identity-signin` / `#topbar-identity-account-info` + `#topbar-identity-signout`, both `hidden` by default, `AccountPanel.js` toggles exactly one based on login state — `TopbarIdentity.js` itself needs no changes, same low-coupling way `CustomStatuses.js`'s rows already coexist in that dropdown); **lobby** — a small self-built `.lobby-account-btn` ("Sign in", or "{username} ▾" opening a one-item "Sign out" `.quick-status-menu`), since no identity element existed there at all. The actual login/register form is a small anchored popover again (`#account-login-popover`, `.quick-settings-popover.topbar-popover` — the same class every other topbar popover in this app uses), inserted as a DOM sibling inside whichever `.quick-status-dropdown`-classed wrapper is relevant (the room's real one, or the lobby's new one) so it inherits that wrapper's existing absolute positioning for free — no fixed-position JS math needed, unlike `RoomRail.js`'s body-appended popover. **Cross-component decoupling needed a new event**: `SocialPanel.js`'s poll `onSessionExpired` callback used to call `this.accountPanel.forceLoggedOut()` directly when it owned that instance; now it dispatches `peek:force-logout`, which `AccountPanel.js` listens for itself — same shape as `peek:account`/`peek:open-dm`.
  - **`SocialPanel.js` no longer clones Settings' shell.** Account tab removed entirely — the trigger button (`#social-button`) simply doesn't exist until `peek:account` reports a logged-in session ("can't have friends without an account", so there's no logged-out fallback view to render). Replaced the `.settings-overlay`/`.settings-nav` full-bleed two-pane layout with a **right-anchored, partial-width drawer** (`.social-drawer`, ~24rem, own lighter `.social-backdrop` instead of an opaque full-bleed one) holding a **segmented Friends/Messages switcher** (`.social-tabs`, reusing the existing `.settings-segmented` class the screen/cam quality pickers already use) instead of a vertical nav-sidebar list. Only `.settings-close-btn` (generic icon-button chrome) and `.settings-segmented` are borrowed from Settings' classes; nothing nav-sidebar/sticky-header-shaped remains. `FriendsPanel.js`/`MessagesPanel.js`'s own logic is untouched — only the wrapper class their `_sectionEl` visibility check looks for changed (`.settings-section` → `.social-tab-panel`).
  - **Gotcha hit while wiring `#topbar-identity-signin`**: it combines `.quick-status-option` (unlayered, sets `display:flex`) with the layered `.hidden` utility — the standing cascade-layers gotcha in this file. Fixed with the same `.foo.hidden{display:none}` compound-override pattern already used for `.quick-status-menu.hidden`/`.quick-settings-popover.hidden`, added as `.quick-status-option.hidden`.
  - **DM composer fix (2026-09-08, owner-reported, screenshot showed a tiny input next to an oversized green button — "unusable"):** the old composer was `<input class="settings-text-input">` next to `<button class="lobby-btn-primary">Send</button>` — mismatched heights by design (a form-field input next to a full-width form-submit button, never meant to sit side by side). Replaced with a real clone of the room's own chat composer: `<textarea class="chat-composer-input">` + an icon-only `.chat-composer-send` button, wrapped in `.chat-composer` (all three classes reused verbatim from `ChatUI.js`'s composer, not reinvented) — same proportions, same rounded-pill look, `MessagesPanel.js`'s `_wireSend()` needed no logic changes since `.value`/`keydown` work identically on a textarea. `_showConversation()` now also sets `this.input.placeholder` to `Message {username}`, matching the room composer's "Message the room" convention. **Hit the exact same cascade-layers gotcha a second time** while making `#messages-inbox-view`/`#messages-conversation-view` fill the drawer's height: the unconditional `display:flex` override was beating the `.hidden` utility on whichever view should have been hidden, rendering both at once — fixed by scoping the selector to `:not(.hidden)`.
  - Verified via a real two-context Playwright pass: register/login through the new popover (both from the room's identity dropdown and the lobby's own button), confirm the drawer has exactly two tabs defaulting to Messages, friend + DM flow, sign-out from each page's identity area actually hides `#social-button` again, and — the actual point of round 1 — opening the drawer from inside a live room with the call staying intact underneath.

### Additional entries moved 2026-09-10, second pass (ninth housekeeping pass)

A deeper re-audit (explicitly run with a larger model, at the user's request, to double-check the same-day eighth pass above) found more bloat the first pass in this session missed. Same treatment: moved verbatim, CLAUDE.md keeps a trimmed rule + pointer. This pass also deleted the "Accounts real-usage audit fixes" Key Conventions entry outright (no move needed — it was pure retrospective with no forward-looking rule, and its content already lives in full both in `CHANGELOG.md`'s Accounts section and in this file's own "Accounts real-usage audit fixes (2026-09-07)" entry above).

- **Incoming noise-suppression keep-alive bug, full repro (2026-09-10)** — the narrative CLAUDE.md's "Incoming (receive-side) noise suppression" entry trimmed down to one rule sentence: **Real bug, found 2026-09-10 (owner-reported: "it mutes them it doesnt do the rnn thing")**: turning this on for a peer produced total silence, not denoised audio — confirmed via a real two-tab Playwright repro measuring actual RMS amplitude (not `getStats().audioLevel`, which is unreliable in this environment per the active-speaker note above — a genuine `AnalyserNode` tap on the audio element's `srcObject` was used instead, and DOES read real samples here). Root cause: turning suppression on repoints the real `audio-${peerId}` element at the *processed* track (`UIController.addAudio()`), which detaches the *raw* remote track from the only `<audio>`/`<video>` element it was ever attached to. **Chromium stops decoding a remote WebRTC audio track once nothing is actually playing it — a Web Audio tap (`createMediaStreamSource`) alone is not enough to keep it flowing** — so the instant suppression switches on, the `NoiseSuppressor`'s worklet input starves and the whole pipeline (including the now-processed track reaching the real element) goes completely silent. None of the existing tests caught this because `noise-suppression-lifecycle.mjs` is pure-logic (no `document`, mocks `_applyIncomingNoiseSuppression` before ever hitting the real enabled-path code) and `participant-context-menu.mjs` only asserts the menu label/badge, never actual audio output — this class of bug (wiring correct, audio silent) is invisible to both. Fixed with `_ensureRawTrackKeepAlive(peerId, rawTrack)`/`_releaseRawTrackKeepAlive(peerId)`: a hidden, muted, autoplaying `<audio>` element per suppressed peer whose only job is to keep the raw track "claimed" by a real media element so Chromium keeps decoding it, created right before the raw track is detached and torn down when suppression turns back off, the worklet fails to load, or the peer disconnects (`removePeer()`). Verified via the same real-RMS Playwright technique: silent before the fix, real denoised amplitude after.
- **Stage toggle, full mechanism (2026-09-09)** — the narrative CLAUDE.md's "Stage toggle" entry trimmed down to two rule sentences: Discord-style "hide the video stage" for a room that's never used for screen/cam sharing — `body.stage-hidden` hides `#videos` (display:none, not width:0) and `#chat` flex-grows into the freed space; `#members-sidebar`'s own resize handle is the one remaining boundary to drag. Close/reopen matches chat/members' own learned pattern rather than a persistent topbar toggle (an earlier version used one — dropped the same day, it read as "control my camera" sitting next to the mic/cam dock buttons): `#stage-close-btn` sits in `#videos`'s own idle/no-stream state, hover-revealed like `.controls-close-btn`; `#toggle-stage-tab` reopens it. **Idle-gated**: `UIController._refreshStageBusyState()` toggles `body.stage-busy` off `Object.keys(this.streams).length` (called from `addStream()`/`removeStream()`) — the close button only shows/works while nothing's streaming, since hiding an in-progress stream would waste the sender's bandwidth on video nobody's watching; CSS hides it and the click handler independently re-checks the same class (a programmatic `.click()` — e.g. `peek-desktop`'s titlebar relay — bypasses CSS visibility, so the JS guard has to stand on its own). Any new stream force-reopens the stage regardless of who started it (`UIController._autoShowStage()`, dispatches `peek:stage-restored`), so a chat-only layout can never silently swallow someone's video. **`#controls` (the mic/cam/share/deafen/record dock) is also a descendant of `#videos`**, and `display:none` has no per-child override the way `visibility` does — `App.js`'s `relocateControlsForStage()` reparents `#controls` (and its own `#controls-tab` reopen affordance, same problem) out to `.room-shell` while the stage is hidden, restoring it to its exact original spot (via a marker comment node, not an assumed DOM position) on the way back. `.room-shell` is already `position:relative` and already inset for the room rail, so `#controls`' own CSS (`width:100%`/`margin-top:auto`, neither parent-dependent) reproduces the same flush-bottom look with no changes, just spanning the wider chat+members combined width. `peek-desktop`'s title bar has a matching "Toggle Video" quick-action button (`toggleStagePanel()`, same two-element non-single-toggle shape as its own controls-dock button) — see that repo's own CLAUDE.md integration-contract table if touching any of the ids/classes involved.
- **Messages as a chat-panel tab, full mechanism (2026-09-09, trial, may be reverted)** — the narrative CLAUDE.md's own entry trimmed down to the constructor contract: DMs moved out of `SocialPanel.js`'s modal into a third chat-panel tab (Chat/Files/**Messages**) — worth keeping an eye on mid-call the way chat itself is, unlike Friends (a rarer, in-and-out task, which stays exactly the modal it was). `MessagesPanel.js` itself needed no logic changes, just two new constructor options — `badgeId` (which element gets the unread count) and `isVisible` (how to tell if this instance is currently on-screen) — so the identical class works mounted in either shape: the lobby still gets `SocialPanel.js`'s original combined Friends & Messages modal (no chat panel there to fold into), while the room's own `SocialPanel` instance is constructed with `hideMessagesTab: true` (Friends-only, no `.social-tabs` switcher) and App.js's `initRoomMessagesTab()` independently constructs a second `MessagesPanel` targeting the new `#messages-tab-content`/`#messages-tab-badge`. `UIController._switchTab()` is now a 3-way switch, dispatching `peek:messages-tab-shown` when it lands on 'messages' (the only way the room's `MessagesPanel` instance learns it just became visible, same CustomEvent-not-direct-reference shape as everywhere else in this app). FriendsPanel.js's "Message" row action still just dispatches `peek:open-dm` — App.js's listener switches the tab **and** closes the Friends modal (a step `SocialPanel.js`'s own version never needed, since Messages used to live inside that same modal). **Bug found and fixed along the way**: `SocialPanel.js`'s trigger (`#social-button`) combines `.topbar-icon-btn` (unlayered, sets `display:flex`) with the layered `.hidden` utility — per the standing cascade-layers rule the unlayered rule always won, so the Friends button was showing regardless of login state in-room since this file was first written (the lobby's `.lobby-icon-btn` doesn't set `display`, which is why it never showed up there); `_setLoggedIn()` now uses `style.display` instead. Guarded by a real two-account Playwright pass (register, friend, join a room, DM through the new tab, confirm the other side's badge lights up and clears on open) — not yet a permanent test in `tests/`, since this is explicitly a trial.
- **Custom avatars, non-security mechanism detail (2026-07-12/13)** — CLAUDE.md's "Custom avatars" entry keeps every security/trust-boundary sentence verbatim (per this project's standing rule never to compress a security paragraph); only this non-security plumbing/history tail was moved out: **Scope cut lifted 2026-07-13**: chat-message avatars used to stay initials-only since `ChatUI.addChatMessage()`/`ensureFileGroup()`/`addPollMessage()` only ever received a resolved nickname *string*, never a peerId. All three now take an optional trailing `senderPeerId` param (every call site in `App.js`/`PeerManager.js` threads the relevant peerId through — the local user's own for a self-echo, the remote connection's peerId for an incoming message) feeding a new `getAvatar` constructor dep (`ChatUI._getAvatar`, wired from `UIController.peerAvatars.get(id)`). `ChatUI._avatarHtml(peerId, initial, color)` is the single choke point deciding image-vs-initials for every message header, reusing the *already-validated* data URL from `peerAvatars` — same trust level as the participant-card/top-bar-pill avatars, and safe to interpolate directly into a template-string `src="..."` attribute since the regex already forecloses attribute-breakout (same reasoning as `img.src` assignment, just via a different DOM API). A poll's `senderPeerId` is a distinct concept from its `myPeerId` param (always *your own* peerId, for vote-tracking, even on someone else's poll) — don't conflate the two. Avatars support transparent backgrounds; the speaking/deafen rings use the same box-shadow-vs-`.avatar-img`-drop-shadow dual-selector split described in "Solid-red mute/deafen dock buttons" below to hug non-square shapes rather than tracing a mismatched square. Two 2026-07-13 bug fixes (own avatar not showing to yourself; transparent backgrounds leaking the initials-color square underneath) are resolved — full history in `ARCHITECTURE.md`.
